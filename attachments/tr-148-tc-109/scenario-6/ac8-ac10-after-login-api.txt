TR-148 / TC-109 · Escenario 6 — AC8 la API after-login entrega sólo si todo pasa · AC10 casos negativos
Reutiliza TR-147 (IM-968 / TC-108) escenarios 1 y 3, y lo AMPLÍA con lo medido en esta corrida.

EVIDENCIA REUTILIZADA — TR-147
--------------------------------------------------------------------------------------------------
  Seis casos ya ejecutados en vivo contra este endpoint con la identidad REAL de la org de VNFP:
      org fuera de scope -> []   · org inexistente -> []   · sin clientOrgId -> []
      sin userCreatedDate -> []  · ya respondida -> []     · caso elegible -> la General
  Y el contrato del launcher (clientOrgId, respondentId, userCreatedDate reales). No se rehace.

AMPLIACIÓN MEDIDA EN ESTA CORRIDA (org de Auctifera, identidad real 005dq00000NLWpIAAX)
--------------------------------------------------------------------------------------------------
AC8 — se entrega SÓLO cuando TODAS las reglas pasan ..................................... PASS

  Caso                                                        | Resultado | ¿Objetivo? | Veredicto
  ------------------------------------------------------------|-----------|------------|----------
  todas las reglas pasan (General 26 activa, org en scope,     |   [26]    |    SÍ      |  PASS
    delay 0 cumplido, sin respuesta previa)                    |           |            |
  MISMO contexto contra el endpoint de FEATURE trigger         |    []     |    SÍ      |  PASS
  org fuera del scope de audiencia (VNFP contra la General 26  |    []     |    SÍ      |  PASS
    acotada a Auctifera)                                       |           |            |
  Start Date no alcanzado (barrido con override de fecha,      |    []     |    SÍ      |  PASS
    escenario 2)                                              |           |            |
  límite de dismissals alcanzado (escenario 3)                 |    []     |    SÍ      |  PASS
  espaciado triesBetweenShows no cumplido (escenario 4)        |    []     |    SÍ      |  PASS

  La SEPARACIÓN DE MODOS es lo que hace este AC no trivial y quedó verificada desde el lado del
  consumidor: una General en modo after-login NO se sirve por el endpoint de feature trigger, y el
  endpoint de feature no la devuelve aunque el contexto sea idéntico.

AC10 — no elegible -> no se muestra nada ................................................ PASS

  El caso más valioso se ejecutó EN SECUENCIA REAL, no por contrato:
      ANTES de responder:  GET general/eligible (identidad real Auctifera) -> [26]
      se envía la respuesta desde el modal del launcher en Giftshop (response id 20)
      DESPUÉS:             la MISMA consulta                              -> []
  "Ya respondió -> no se muestra nada" verificado en vivo.

  Diagnóstico del servidor sobre la identidad real, nombrando la razón exacta:
      surveyId 25 -> eligible:false · reason: "already_answered"
      surveyId 26 -> eligible:false · reason: "already_answered"   (tras responder)
  Y sobre un respondent fresco de la misma org: eligible:true. La diferencia es la identidad, no la
  org ni la fecha — se aisló con 4 sondas (A/B/C/D) antes de concluirlo.

  FAIL-CLOSED confirmado: los casos de contexto incompleto devuelven 200 con lista vacía, nunca
  5xx. "No elegible" es una respuesta normal, no un error.

VEREDICTO ESCENARIO 6: PASS (AC8 y AC10)
