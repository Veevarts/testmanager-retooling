@@ -4,17 +4,17 @@ DECLARE @filterDateFrom DATE = CAST(@dateFilterFrom AS DATE);
 DECLARE @filterDateTo DATE = CAST(@dateFilterTo AS DATE);
 
 SELECT
-    CAST(rr.ID AS VARCHAR(36)) AS Implementation_External_ID__c,
-    CAST(ft.ID AS VARCHAR(36)) AS Opportunity_Implementation_External_ID__c,
-    CAST(rr.REVENUESPLITID AS VARCHAR(36)) AS Revenue_Split_ID_legacy__c,
-    CAST(rr.CONSTITUENTID AS VARCHAR(36)) AS Recognized_Constituent_ID__c,
+    rr.ID AS Implementation_External_ID__c,
+    ft.ID AS Opportunity_Implementation_External_ID__c,
+    rr.REVENUESPLITID AS Revenue_Split_ID_legacy__c,
+    rr.CONSTITUENTID AS Recognized_Constituent_ID__c,
     recognized_c.LOOKUPID AS Recognized_Constituent_LookUp_ID__c,
-    CAST(ft.CONSTITUENTID AS VARCHAR(36)) AS Donor_Constituent_ID__c,
+    ft.CONSTITUENTID AS Donor_Constituent_ID__c,
     donor_c.LOOKUPID AS Donor_Constituent_LookUp_ID__c,
     rse.APPLICATION AS Application,
     rr.AMOUNT AS Recognition_Amount__c,
     CAST(rr.EFFECTIVEDATE AS DATE) AS Recognition_Date__c,
-    CAST(rr.REVENUERECOGNITIONTYPECODEID AS VARCHAR(36)) AS Recognition_Type_ID__c,
+    rr.REVENUERECOGNITIONTYPECODEID AS Recognition_Type_ID__c,
     rrtc.DESCRIPTION AS Recognition_Type__c,
     recognized_c.ISORGANIZATION AS Recognized_Is_Organization__c,
     recognized_c.ISGROUP AS Recognized_Is_Group__c
@@ -46,6 +46,6 @@ WHERE
         OR CAST(rr.EFFECTIVEDATE AS DATE) <= @filterDateTo
     )
 ORDER BY
-    CAST(ft.ID AS VARCHAR(36)),
-    CAST(rr.REVENUESPLITID AS VARCHAR(36)),
-    CAST(rr.ID AS VARCHAR(36));
+    ft.ID,
+    rr.REVENUESPLITID,
+    rr.ID;
