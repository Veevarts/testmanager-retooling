@@ -39,11 +39,11 @@ WHERE
     AND recognized_c.ID IS NOT NULL
     AND (
         @useDateFrom = 0
-        OR CAST(rr.EFFECTIVEDATE AS DATE) >= @filterDateFrom
+        OR rr.EFFECTIVEDATE >= @filterDateFrom
     )
     AND (
         @useDateTo = 0
-        OR CAST(rr.EFFECTIVEDATE AS DATE) <= @filterDateTo
+        OR rr.EFFECTIVEDATE < DATEADD(DAY, 1, @filterDateTo)
     )
 ORDER BY
     ft.ID,
