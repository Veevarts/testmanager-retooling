attendance_direct_registrant.sql:
  CTE `current_attendance_key` defines (EVENTID, CONSTITUENTID) pairs from current attendance output.
  Eligible CTE applies:
    AND NOT EXISTS (
      SELECT 1 FROM current_attendance_key cak
      WHERE cak.EVENTID = r.EVENTID AND cak.CONSTITUENTID = r.CONSTITUENTID
    )
Composite key anti-join confirmed. PASS.
