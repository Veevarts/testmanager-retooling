Live safesql on aspen — grain stability check:
Query: GROUP BY REGISTRANT.ID HAVING COUNT(*) > 1 on safe_direct CTE
Result: duplicated_registrant_ids = 0
Static analysis: REGISTRATIONINFORMATION joins use FOR XML PATH scalar subqueries, not row-multiplying INNER JOINs.
EVENT join is 1-to-1 by REGISTRANT.EVENTID.
PASS.
