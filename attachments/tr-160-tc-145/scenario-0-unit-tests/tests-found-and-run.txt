Unit-test inspection — TR-160 / TC-145 / IM-1165
Executed 2026-08-14 (evidence from PR heads + CI status; no local repo clone).

## Files with test coverage in the diff

survey-tool #38:
  · server/src/routes/apiBoundary.test.ts (69721 bytes, 1775 lines, ~36 `it()` blocks)

AuctiferaDX #2580:
  · force-app/main/default/lwc/npsWidget/__tests__/npsWidget.test.js (10805 bytes)

NFPFundraising #2232:
  · force-app/main/default/lwc/npsWidget/__tests__/npsWidget.test.js (10849 bytes)

survey-tool #43 (OPEN): no test files modified.

## CI status at PR head

  · Veevarts/survey-tool #38 (MERGED)   → "Server tests + client build" PASS · "Validate Survey Tool infra" PASS
  · Veevarts/survey-tool #43 (OPEN)     → "Server tests + client build" PASS
  · Veevarts/AuctiferaDX #2580 (MERGED) → no checks configured on the branch (repo hygiene gap; not an IM-1165 defect)
  · Veevarts/NFPFundraising #2232 (MERGED) → no checks configured on the branch (same as above)

## AC → test coverage matrix

| AC   | Test file(s) that cover this AC                                                  | Coverage | Notes |
|------|-----------------------------------------------------------------------------------|----------|-------|
| AC1  | apiBoundary.test.ts — none directly on `POST /sync-salesforce`                    | GAP      | QA covers via live SOQL vs `/api/admin/organizations` count on prd (see scenario-1). |
| AC2  | apiBoundary.test.ts — none directly                                               | GAP      | QA covers by cross-count (SF 197 − Survey Tool 186 = 11 absent AC2 candidates). |
| AC3  | apiBoundary.test.ts:597 "prioritizes a global general survey for active SF org ids without a subdomain" | PASS | Direct. |
| AC4  | aux_npsWidget.test.js (+ NFP equivalent) — assert URL contains `salesforceOrgId=` | PASS | Two positive tests per package. |
| AC5  | apiBoundary.test.ts:215 "requires Salesforce Org ID for manual organizations when SURVEY_TOOL_ENV=production" | PASS | Direct. |
| AC6  | apiBoundary.test.ts:177 partial (env resolution) — none directly on XOR rejection | GAP      | QA covers via live POST on dev (both fields → 400 XOR). |
| AC7  | none directly on regex validation                                                  | GAP      | QA covers via 5 negative POSTs on dev (see scenario-7). |
| AC8  | apiBoundary.test.ts — dozens of subdomain-based setups (lines 574, 731, 819, ...) | PASS     | Every legacy subdomain test flow still passes. |

## Verdict

- 4/8 ACs directly covered by dev unit tests (AC3, AC4, AC5, AC8).
- 4/8 ACs have coverage GAPS in the PR — QA compensates by live validation on prd (AC1, AC2) + live POST on dev (AC6, AC7).
- No failed tests on any PR branch.
- Dev did not add integration tests for the sync-salesforce endpoint itself (AC1);
  this is a follow-up recommendation, not a blocker.
