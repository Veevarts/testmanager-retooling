AC5 Production manual form + server guards — live verification against
https://surveytool.prd.veevart.ai/admin/organizations
Executed 2026-08-14 (env: production).

## Form audit (DOM inspection after clicking "New organization")

Environment banner: production
Fields rendered (in visual order):
  1. Name * (text, required, placeholder "e.g. Museo de Arte de Bogota")
  2. Salesforce Org ID 18 * (text, required, placeholder "00Dxxxxxxxxxxxxxxx")
  3. Active (checkbox, checked by default)

Fields NOT rendered in prd:
  · Subdomain input               (hidden as required by AC5)
  · Identifier type select        (irrelevant in prd — only Org ID 18 is valid)
  · Domain input                  (removed entirely; stronger than AC5 requires)

Screenshot: prd-new-org-form.png (this folder).

## Server guard (defense in depth) — POST /api/admin/organizations behind admin session

| # | Sent body                                                                    | HTTP | Server error                                                    | Verdict |
|---|------------------------------------------------------------------------------|------|-----------------------------------------------------------------|---------|
| 1 | { name: qa-tc145-prd-negative }                                              | 400  | salesforceOrgId is required in production                       | PASS    |
| 2 | { name: qa-tc145-prd-subd, subdomain: qa-tc145-should-be-rejected }          | 400  | salesforceOrgId is required in production                       | PASS    |
| 3 | { name: qa-tc145-prd-17, salesforceOrgId: 00D11111111111111 }                | 400  | salesforceOrgId must be 18 characters and start with 00D        | PASS    |
| 4 | { name: qa-tc145-prd-bad, salesforceOrgId: AAA111111111111111 }              | 400  | salesforceOrgId must be 18 characters and start with 00D        | PASS    |

Highlights:
  · Case 2 proves that even if a client bypasses the form and posts `subdomain` directly,
    the server still refuses (subdomain is normalized to null in production per
    server/src/routes/organizations.ts:37).
  · No writes were made to production. Every 400 fired at validation, BEFORE the
    `db.insert(...)` call in organizations.ts:140.
  · Server implementation: `validateManualOrganization` in
    server/src/routes/organizations.ts:34-67 (regex SALESFORCE_ORG_ID_RE line 9).

Verdict: AC5 PASS (UI hides subdomain + server enforces `salesforceOrgId is required
in production` + format regex enforced).
