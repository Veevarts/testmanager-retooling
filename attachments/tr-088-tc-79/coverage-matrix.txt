Coverage matrix — TC-79 (IM-964 Cognito deployment docs)

PR commit: c4f2a164 (PR #22)
Docs-only PR (+109/-2 in README.md).

AC → doc review:

AC1 — New developer can configure Cognito locally by following the documentation only
  - README "Required Cognito values" section enumerates: Cognito domain, User Pool ID, App Client ID, App Client secret, callback URLs (local/dev/prod), logout URLs.
  - "Server environment variables" section lists AUTH_COOKIE_SECRET, COGNITO_DOMAIN, COGNITO_CLIENT_ID, COGNITO_REDIRECT_URI, COGNITO_LOGOUT_URI, COGNITO_ISSUER, AUTH_SESSION_TTL_SECONDS, COGNITO_ADMIN_GROUPS.
  - "Local development" subsection has 4 numbered steps: add callback URLs to Cognito, create .env, set AUTH_COOKIE_SECURE=false, npm run dev.
  → Covered ✅

AC2 — Production deployment checklist enumerates every Cognito value and callback URL
  - "Deployment checklist" with 9 bullets: Cognito domain active, User Pool ID matches issuer, App Client ID matches, App Client secret in Secrets Manager if used, callback/logout URLs registered, AUTH_COOKIE_SECRET >=32 chars and stored as secret, AUTH_COOKIE_SECURE=true for HTTPS, AUTH_COOKIE_SAME_SITE=Lax default, admin allowlist configured.
  - "AWS dev deployment" subsection documents ECS cluster/service/URL, Secrets Manager path `dev/platform/survey-tool`, SSM Parameter Store paths.
  → Covered ✅

AC3 — No real secrets are committed in the documentation
  - grep against PR diff for secret patterns: only placeholders found:
      AUTH_COOKIE_SECRET=replace-with-at-least-32-random-characters
      COGNITO_CLIENT_SECRET=... # required only when the app client has a secret
      "App Client secret": required only when ...
      placeholder <production-domain> in callback/logout URLs
  - No real Cognito User Pool, no real client secret, no real cookie secret.
  → Covered ✅

AC4 — Cookie security flags documented with right values
  - Deployment checklist: AUTH_COOKIE_SECURE=true (HTTPS), AUTH_COOKIE_SAME_SITE=Lax.
  - Local development: AUTH_COOKIE_SECURE=false (justified: plain HTTP via Vite).
  - HttpOnly is enforced server-side and not a configurable env-var (documented implicitly via "session cookie" framing).
  → Covered ✅

AC5 — Local development callback URL documented and distinct from production
  - Required Cognito values section: Local=http://localhost:5173/api/auth/callback, Dev=https://surveytool.dev.veevart.ai/api/auth/callback, Production=https://<production-domain>/api/auth/callback.
  - Same triple for logout URLs.
  - "Local development" step 1 reiterates registering the local callback in Cognito.
  → Covered ✅

Gaps: None. All 5 ACs verifiable from the diff alone.
Optional smoke test (boot the server locally with the doc-only instructions and a test Cognito User Pool) is NOT auto-executable without a Cognito User Pool we don't have. Marked as `not-run` with manual handoff note.
