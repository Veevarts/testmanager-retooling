# IM-560 / TC-113 — security regression scan over PR #152 diff (74edd98e..1f3ac47b)

## Secrets / credentials / keys in the diff:
+const shouldRunDocker = process.env.RUN_DOCKER_SQL_QUERY_TESTS === '1';
+const testSaPassword = `Aa1!${randomUUID().replace(/-/g, '')}`;
+  // Record-type template tokens are resolved by the app layer; any literal works
+  return sqlText.replace(/\{\{[A-Z_]+\}\}/g, 'RECORD-TYPE-TOKEN');
+          MSSQL_SA_PASSWORD: testSaPassword,
+        password: testSaPassword,
+    const flagEndToken = 'END AS HasMembershipTransaction';
+    const flagEnd = normalized.indexOf(flagEndToken, flagStart);
+      flag: normalized.slice(flagStart, flagEnd + flagEndToken.length),
  → (empty above = no secrets introduced)

## Dynamic SQL / string-concatenation injection surface (added lines):
  → (empty above = no dynamic SQL / concatenated user input added)

## PII columns newly SELECTed to output (names/emails/phones):
  → (empty above = no new PII surfaced by the queries)
