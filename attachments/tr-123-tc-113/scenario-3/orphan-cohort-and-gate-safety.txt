IM-560 / TC-113 · Scenario 3 — orphan cohort measurement + gate-narrowing safety on REACHABLE orgs

The High Desert orphan RECOVERY cardinality (13 Opps / $34,426 · 10 emit / 3 net / 3 POS)
could NOT be reproduced: no safesql profile exists for high-desert, and every reachable org
has ZERO orphans (measured below), so the orphan data simply does not exist on aspen/illinois/tucson.
Instead QA verified, on the reachable orgs, that the orphan-handling is correctly identified and
the gate narrowing is regression-safe.

## Orphan cohort — independent measurement (donation split whose source membership line has no active MT)

Profile | donation-on-membership-line splits | split-receipting (MT-backed) | ORPHANS (no MT) | orphan $
aspen        | 11 | 11 | 0 | $0
illinois_2   | 0  | -  | 0 | $0
tucson       | 0  | -  | 0 | $0

→ aspen: all 11 donation-on-membership splits are MT-backed (= the S2 split-receipting cohort);
  ZERO orphans. Confirms the dev's "aspen has no orphans" claim. Orphans are High Desert-specific.

## Gate-narrowing safety — dev's REAL queries pre-fix (74edd98e) vs post-fix (1f3ac47b) on aspen

Query                          | PRE    | POST   | Delta | Interpretation
donation_transaction.sql       | 3,925  | 3,925  |   0   | gate narrowing is a NO-OP on aspen (0 orphans → no donation Opp added/removed). Matches dev-signed 3,925. Zero regression.
fund_assignment_donations.sql  | 3,929  | 3,929  |   0   | the 11 split-receipting lines are NOT emitted here (excluded both pre & post as MT-backed) → NO double-count.
fund_assignment_memberships.sql| 10,212 | 10,223 | +11   | the new SplitReceiptingDonationLines branch adds EXACTLY 11 FA. Matches dev-signed 11 and the S2 independent aggregate.

## Conclusion for S3
- Recovery MECHANISM: proven regression-safe (dt/fad unchanged) + additively correct (fam +11 exact) on aspen;
  the orphan-inclusion contract is asserted by the static gate spec (13/13, scenario 5).
- Recovery CARDINALITY for High Desert (13/$34,426) remains DEFERRED — requires a high-desert safesql profile
  or Docker up for the dockerized fixture. Not reproducible on any org QA can reach (all have 0 orphans).
