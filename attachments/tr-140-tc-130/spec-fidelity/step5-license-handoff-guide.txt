{
  "guide": {
    "context": "The previous automated step assigns both cloned permission sets ('Veevart2' and 'Veevart NonProfit2') to every active user WHEN the running org holds the Veevart license those permission sets require. Where the license is absent - a pre-provisioning org, or users on a license that does not permit the managed components (e.g. Visualforce access) - Salesforce REJECTS the assignment and the automated step DEFERS that permission set to this handoff instead of failing the run. If the automated step reported NO deferrals, both clones are already assigned to all users and this is a quick verify-and-confirm; otherwise assign the deferred permission set(s) here, after Veevart Ops provisions the required license.",
    "actions": [
      "Open the automated 'Assign the cloned permission sets' step log: any permission set listed as DEFERRED (license absent) still needs assignment; if none were deferred, all users are already assigned - verify and mark done.",
      "If a Veevart PermissionSetLicense is required but missing/inactive for the org, ask Veevart Ops to provision and activate it before assigning.",
      "Setup > Permission Sets > open 'Veevart2' > Manage Assignments > Add Assignments > select all active users > Next > No expiration date > Assign.",
      "Setup > Permission Sets > open 'Veevart NonProfit2' > Manage Assignments > Add Assignments > select all active users > Next > No expiration date > Assign.",
      "VERIFY: each cloned permission set's Manage Assignments list includes every active user; mark done only when both clones are assigned to all users (or confirmed already assigned by the automated step)."
    ],
    "estimatedMinutes": 10
  }
}