Resultados de la matriz de regresion
====================================

TECNICA — 10 de 10 con veredicto, sin regresiones
  1-9 PASS, con la fuente de cada uno en la carpeta del escenario correspondiente.
  El item 10 no es un PASS/FAIL sino una confirmacion: convertir no tiene vuelta atras por
  tres caminos distintos (ver scenario-7/). No es defecto porque los criterios no pidieron
  reversibilidad; se reporta como consecuencia de diseño que el producto no advierte.

  El item 8 merece una nota: declinar es la otra mitad del mismo control y comparte
  endpoint de lectura, asi que se ejercito de ida y vuelta (decline -> accept rechazado ->
  restore) para comprobar que la historia hermana IM-1234 sigue sana despues de este
  cambio. Lo esta.

SEGURIDAD
  S1  ABIERTO y PREEXISTENTE, no introducido aqui. `ownerId` viene del que llama y solo se
      valida el grupo del equipo, asi que un miembro puede accionar la cola de otro CSM.
      Esta corrida lo uso deliberadamente: sesion admin, actuando sobre la cola de
      Valentina Aray mediante el selector "View as" que el producto ofrece para eso.
      Es el modelo de producto vigente, declarado en el backlog del repo, y su cierre pide
      cambio de contrato. Se registra, no se abre defecto.
  S2  PASS. El chip de fuente abre un correo real con direcciones del cliente y del CSM.
      Esa pantalla NO se capturo y su contenido NO se transcribio. La evidencia guarda
      unicamente el hecho de que el modal abrio y la referencia opaca de la fuente.
  S3  PASS. El modal de fuente mostro el correo que la referencia de la fila apunta, y
      nada mas; no hay forma observable de pedirle otro documento desde la tarjeta.
  S4  PASS. Ni el diff de la historia ni esta evidencia contienen credenciales ni tokens.
      El token de sesion se uso desde el navegador y no se copio a ningun archivo.
