Tests del repo que cubren la rebanada de IM-1235
=================================================

BACKEND — harness ejecutado contra Postgres real
  scripts/csm-os/verify-task-suggestions-local.ts contiene un bloque dedicado a esta
  historia: "IM-1235 AC4: an accepted task keeps its source chips", con cinco checks que
  prueban justo lo que el criterio pide — misma fila, mismos chips, misma referencia de
  fuente y mismo id despues de aceptar. Su salida esta en im-1235-ac4-checks.txt.

  PROCEDENCIA DE ESA EVIDENCIA, dicha claramente: el harness se ejecuto completo ayer en
  la corrida de TC-174 / TR-191 (347/347 contra un Postgres real levantado desde las
  migraciones del mismo arbol) y ese log esta comiteado en
  attachments/tr-191-tc-174/scenario-0-harness/. El backend NO cambio desde entonces —
  el unico PR de ayer a hoy es el #203, que es solo frontend — asi que esos checks
  corresponden al mismo codigo que se probo hoy en vivo. No se re-ejecuto para no fabricar
  una corrida nueva que diria exactamente lo mismo.

  En esa misma corrida se probo ademas que esos checks PUEDEN fallar: tres mutaciones
  distintas los pusieron en rojo (ver attachments/tr-191-tc-174/scenario-0-harness/
  mutation-matrix.txt).

FRONTEND
  La suite completa del repo se ejecuto hoy, en el commit del fix de IM-1234 (167bcae):
  2611 tests PASS, 9 skipped, y 2 FAIL ajenos a esta historia
  (src/lib/staleChunkReload.test.ts, preexistentes desde el "PR #201" — detallados en
  attachments/tr-191-tc-174/scenario-3/retest-2026-09-22/pre-existing-failures.txt).
  Ninguno de los dos toca el panel de sugerencias ni la conversion.

COBERTURA POR CRITERIO — donde se apoya cada uno
  AC1  la accion existe en cada sugerencia   | sin test automatico; QA lo midio en la UI
  AC2  la tarea llega con todo               | harness (misma fila) + diff de 25 campos de QA
  AC3  sale de la lista y queda ligada       | harness (id estable) + conteos medidos en vivo
  AC4  volver al origen                      | harness "keeps its source chips" + UI en vivo

  HUECO: no hay ninguna prueba automatica de que la ACCION este presente en la interfaz
  (AC1). Si alguien quitara el boton de una de las dos superficies, ninguna suite lo
  detectaria. Es el mismo tipo de hueco que la historia hermana IM-1234 tuvo con la
  etiqueta de la pestaña Deleted, y se cubre igual: con una asercion e2e donde si hay
  navegador.
