Docker integration suite — DEFERRED (same colima startup-race issue documented in TR-169/170).

Local this session: 13/13 FAIL, all with "Log stream ended and message /.*Started.*/ was not
received". Colima + mssql-server 2022 boots in ~90s under load; testcontainers default log-wait
is 60s. Root cause is local env resource contention, NOT a defect in the PR.

Substituted with:
  - jest static specs (rental-event-query.spec.ts): 37/37 PASS on PR branch, including 13 new
    IM-1211 tests covering merge, labels, HTML escape, title-only notes, empty-source handling,
    ordering, itinerary pre-aggregation, and 4 guard-helper meta-tests.
  - Full query-regression suite: 580/580 PASS · 168 skipped · 46/63 suites.
  - Revert-proof of the main SELECT projection (see revert-proof.txt).

Dev-reported RUN_DOCKER_SQL_QUERY_TESTS=1 per PR body: all live-database tests PASS.
