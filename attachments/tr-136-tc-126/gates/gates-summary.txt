IM-1087 / TR-136 — gate transcript summary (MigrationAppBackend @ 5f1fdd1d, base a2ee3cbf)
==========================================================================================
Environment: node v25.4.0. Deps reused from the 82c35b14 clone; package.json/lock UNCHANGED 5f1fdd1d..c671b71f (verified).

tsc --noEmit ....................... exit 0, 0 diagnostics                    [tsc-noemit.log — empty]
eslint (touched .spec.ts) .......... exit 0, 0 problems                       [eslint-touched.log — empty]
jest ic-3-25 lockstep .............. 1 suite, 6 tests, exit 0                [jest-ic-3-25.log]
  ✓ parses against the publish-plan-template CLI schema (v2, hybrid)
  ✓ keeps case.version and template.version in lockstep
  ✓ has 4 ordered steps: 2 AUTOMATED T3 settings then 2 MANUAL handoffs
  ✓ Step 1 disables the Enhanced Profile UI (UserManagementSettings.enableNewProfileUI=false)
  ✓ Step 2 enables admin login-as-any-user (SecuritySettings.enableAdminLoginAsAnyUser=true)
  ✓ keeps user creation and permission-set assignment as structured MANUAL guides
jest seeds/specs (18 files) ........ 18 suites, 167 tests, exit 0            [jest-seeds-suite.log]
jest seeds/ PARENT dir ............. 19 suites, 189 tests, exit 0            [jest-seeds-parent.log]
jest FULL (src + scripts) .......... 188 pass / 189 suites (1 skipped); 1977 pass / 1978 tests (1 skipped), 0 fail  [jest-full.log]

DEV-CLAIM RECONCILIATION: PR body "jest seeds suite 19/19 (189 tests) green". Reproduced EXACTLY when jest is pointed at
the seeds/ PARENT dir (seeds/specs/ = 18 + seeds/publish-plan-template.cli.spec.ts = 1 -> 19 suites / 189 tests). My first
run on seeds/specs/ alone gave 18/167; the +1 suite / +22 tests is the CLI spec one directory up. Full = 1977/1978, 0 fail.
tsc + eslint clean. Reported what I obtained.

OP-REUSE (NOT genuinely-new): #259 changed 2 files (spec .json + .spec.ts), NO adapter/domain-model/handler. The T3
`settings` op + UserManagementSettings/SecuritySettings types pre-exist at base a2ee3cbf. enableAdminLoginAsAnyUser is a
new *setting value string* in the spec (not new code) — the generic settings op needs no per-field change. See
spec-fidelity/op-reuse-and-master-audit.txt.

MASTER-INCLUSION OBSERVATION (documented, not an AC failure): the PR body's "pin ic-3-25 into WORKING_CASE_IDS" is NOT in
#259's diff; at #259 the hybrid 3.25 is silently dropped from the master. IM-1090/#262 later made selection predicate-based
(TR-130), so on current main 3.25 IS in the master. Surfaced to dev/PM. Full detail in the audit file.
