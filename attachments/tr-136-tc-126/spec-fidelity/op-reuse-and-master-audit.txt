IM-1087 / #259 (5f1fdd1d, base a2ee3cbf) — op-reuse + master-inclusion audit
============================================================================

FILES CHANGED base..merge (2):
  src/implementations/infrastructure/seeds/specs/ic-3-25-setup-profiles-and-users.json    (v1->v2, +62/-15)
  src/implementations/infrastructure/seeds/specs/ic-3-25-setup-profiles-and-users.spec.ts (new lockstep, +96/-0)
  => spec-only. NO adapter / domain-model / handler file changed.

OP-REUSE (T3 `settings` op is REUSED, NOT genuinely-new):
  adapter files changed by #259 ......... NONE
  keys present at base a2ee3cbf:
    UserManagementSettings ...... 4 src files   (op + type pre-exist)
    SecuritySettings ............ 5 src files   (op + type pre-exist)
    enableNewProfileUI .......... 3 src files   (already used by IC 3.05)
    enableAdminLoginAsAnyUser ... 0 src files   (a NEW *setting value string* in the 3.25 spec JSON, NOT new code —
                                                 the generic T3 settings op deploys arbitrary settingsType+values,
                                                 so a new SecuritySettings field needs NO adapter change; #259 changed none)
  => the T3 settings mechanism is fully reused; only spec DATA is new. Correctly classified spec-only / reused-op.

MASTER-INCLUSION — the one finding worth surfacing (NOT an AC failure, documented OBSERVATION):
  The PR body says: "scripts/seed-master-template.ts: pin ic-3-25 into WORKING_CASE_IDS so the hybrid case stays
  in the master (matches 4.05/8.02 precedent)."  This is NOT honored in #259's diff:
    - #259 does NOT touch scripts/seed-master-template.ts at all.
    - WORKING_CASE_IDS @ 5f1fdd1d = [company-info-and-mock-contact, set-up-general-donation-fund,
      ic-4-01-specific-funds-debit-credit, ic-4-05-transaction-journal-layouts, ic-8-02-resources-setup].
      ic-3-25 is ABSENT (grep count 0). 4.05 and 8.02 ARE present — so the "precedent" is real but 3.25 was never added.
  Consequence AT #259 in isolation: master selection = WORKING_CASE_IDS  U  {feasibility=='manual' manual-tier cases}.
    3.25 v2 is feasibility='hybrid', so the manual-tier predicate no longer picks it up, and it is not pinned ->
    the freshly-upgraded hybrid 3.25 is SILENTLY DROPPED from the master (the exact "silent-drop-on-flip" failure mode).
  Resolution (later, same epic): IM-1090 / #262 (merge 82c35b14, "master-template policy", QA'd in TR-130) REWROTE
    seed-master-template.ts to PREDICATE-BASED selection — every auto/hybrid case is always included (EXCLUDE_FROM_MASTER
    = only ic-veevart-auth), no pin-list to maintain. So on CURRENT MAIN the 3.25 hybrid IS correctly in the master,
    admitted by the predicate — NOT by the (never-applied) #259 pin.
  Net: 3.25's ACs (the toggles + manual handoffs) are correct and unaffected; the spec upgrade is sound. But the #259
    PR-body master-pin claim was inaccurate, and the actual master inclusion depended on a SIBLING PR (IM-1090/#262),
    not on #259. Surfaced to dev/PM as an observation (correct the body / confirm no residual gap on any non-predicate path).

VERSION CHAIN (not a defect): #259 = 3.25 v2; IM-1087a/#270 (merge 6c0614dd) = 3.25 v3 (hardened manual permset guide).
  This QA evaluates the v2 at #259; v3 is a documented follow-up.
