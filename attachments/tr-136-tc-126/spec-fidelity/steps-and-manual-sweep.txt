IM-1087 — IC 3.25 v2 spec-fidelity (independent parse) + 7 stay-manual review
=============================================================================
Spec @ 5f1fdd1d: ic-3-25-setup-profiles-and-users, case.version=2, template.version=2, feasibility=hybrid, defaultOrder=325.

STEP-BY-STEP (independent parse of the JSON, not the lockstep):
  step1 AUTOMATED tool=T3 op=settings  UserManagementSettings values={enableNewProfileUI:false}   -> AC1
        "Disable the Enhanced Profile User Interface"  (verify read-back false; idempotent)
  step2 AUTOMATED tool=T3 op=settings  SecuritySettings       values={enableAdminLoginAsAnyUser:true} -> AC2
        "Enable 'Administrators Can Log in as Any User'"  (verify read-back true; values contains ONLY this key
         => no other security/session setting modified)
  step3 MANUAL  "Create the client's users and grant instance access"          -> AC3 (license + First/Last name)
  step4 MANUAL  "Assign the Veevart permission sets to each user"              -> AC3 (5 permission sets)
=> matches the 3 Gherkin ACs exactly (AC1=step1, AC2=step2, AC3=steps3+4).

7 STAY-MANUAL findings — QA soundness review (rationale defensible? each names a concrete blocker):
  3.19 Branding & Theme .......... SOUND. BrandingSet/LightningExperienceTheme are deployable types, but ThemeItem values
                                    are client binary/hex (logo/banner/avatar = ContentAsset uploads). No org-agnostic slice.
  3.11 Create All Documents ...... SOUND. Managed "Run Scripts" Apex wizard has no callable/stable entry point; User-object
                                    FLS + page layout not API-drivable; Canadian tax-receipt content is legal/client copy.
  3.15 Custom Apps & Branding .... SOUND. CustomApplication.brand.headerColor deployable, but color is a per-app client
                                    value read off the client site; app logo+favicon are ContentAsset uploads; clone-from-demo
                                    is a cross-org wizard.
  3.32 End of Day Dashboard ...... SOUND. Pure managed-package install (04tQH000001Apbl). Tooling PackageInstallRequest is a
                                    real future path but heavyweight/org-mutating and has no op — documented, out-of-scope to run.
  5.24 Tax Letter Set-up ......... SOUND. Paragraph field API names are namespace/org-variable (playbook names vnfp__…; the
                                    verify org's Document Builder is Auctifera__-namespaced and those fields are absent) ->
                                    not org-agnostic / unverifiable; list-view button has no op; signature/CRA labels are content.
  5.25 Digital Membership Cards .. SOUND. Add-on requiring explicit client sign-off (must NOT auto-run in the master); the
                                    automatable slice is thin (a feature flag + one layout field); value-bearing parts are UI/file.
  8.06 Sandbox Creation .......... SOUND. Heavyweight/async, explicitly out-of-scope to run; Tooling SandboxInfo is the future
                                    path but is unsupported on the DE verify org; post-create steps are in-sandbox/wizard bound.
=> All 7 name a concrete, defensible blocker (ContentAsset upload / managed wizard / namespace-variable field / unsupported
   or heavyweight tooling). No org-agnostic automatable slice was left on the table. The spike's manual-confidence outcome
   (1 upgrade, 7 stay manual) is sound. Live probes (Tooling/CamiloDevOrg) were dev-only; QA validated plausibility + catalog fit.
