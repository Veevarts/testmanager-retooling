Command:
node backfill-manual-analytics-runs.cjs \
  --api-key SECRET_TO_LEAK \
  --start-date 2026-01-01 --end-date 2026-05-31 \
  --org-file org-ids-with-dupes.js \
  --dry-run

stderr: Do not pass API keys via --api-key; use MANUAL_EXTRACTION_API_KEY.
Exit code: non-zero.

Analysis:
- AC6 PASS: el script REJECTS API key passed via CLI. El error es claro y explica el alternativo (env var). SECRET_TO_LEAK NO aparece en stdout/stderr (solo el nombre del flag prohibido). Fast fail antes de cualquier procesamiento.
