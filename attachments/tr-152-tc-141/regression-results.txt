IM-741 / TC-141 — resultados de regresión (ejecutado 2026-08-04)

TÉCNICA
  T1 PASS  Quitar TIER_1_CORE (VNFP) -> 6 tests fallan. Quitar TIER_2_PLATFORM -> 1 falla.
           Ningún tier es decorativo.
  T2 PASS  Romper normalizeName (volver a comparación exacta) -> 5 tests fallan. Es la guarda más
           importante de las tres: una raíz no reconocida NO da error, solo se ordena mal, así que
           sin este test el bug reaparecería en silencio.
  T3 PASS  Reinyectar el attempt en jobIdempotencyKey -> 1 test falla. El invariante del AC4 está
           lockeado por un test, no solo por el comentario.
  T4 PASS  isPackageVersionId valida por PATRÓN, no por "string no vacío", que es lo que hace que
           el centinela de cuatro caracteres "null" sea rechazado. Las cuatro formas (null real,
           "null", cadena vacía, registry vacío) están cubiertas por el spec del dev.
  T5 PASS  Cubiertos los dos lados: "requeues a retryable ERROR once" y "does not requeue a
           non-retryable ERROR".
  T6 PASS  `notAttempted` produce "not attempted: earlier package <X> failed" — nombra al culpable.
           Tres estados distintos y no colapsados: skipped / failed / not attempted.
  T7 PASS  Suite completa sobre main @ 11f95fab: 232 suites / 2747 tests PASS, 1 skipped, 0 fallos.
           ⚠️ CIFRA DEL DEV RE-DERIVADA: el ticket reporta "185 suites / 1993 tests". Esa cifra es
           de la época de #306; hoy la suite es 232/2747. No es un error del dev, es una cifra
           vieja — pero se re-derivó en vez de copiarse.
  T8 PASS  ic-3-01 declara los tres pasos que efectivamente corrieron: NPSP manual por MetaDeploy,
           instalación de paquetes Veevart (T1), verificación de namespaces (T6).
  T9 PASS con precisión. El dev reporta NpspInstall-SM como "deployed but currently unrouted".
           run-sm.asl.json SÍ la cablea: Choice[1] -> NpspInstallSMInvoke con la condición
           kind==AUTOMATED AND tool==T3 AND inputPayload.op==installPackages.
           Está viva; hoy no se alcanza sólo porque el catálogo no declara ningún step así.
           "Cableada pero inalcanzable" ≠ "sin rutear". No es defecto; es una imprecisión que
           importa porque nadie debería tratarla como código muerto en un refactor.
  T10 FUERA DE ALCANCE  Verificar la re-corrida exige disparar el installer, y la única org con la
           instalación completa es de PRODUCCIÓN. QA no ejecuta escenarios que requieran tocar una
           org productiva, así que esto no es un item bloqueado sino uno que no le corresponde a QA.
           Queda como entrega del dev/PM. Ver ac7-excluded-prod-only.txt.

SEGURIDAD
  S1 PASS  No hay credenciales en el código del installer. El ticket lo exige explícitamente
           ("Lambda holds only short-lived credentials") y el adapter lo respeta.
  S2 PASS  La api key se lee de Secrets Manager por invocación y no se almacena.
  S3 PASS  `submitInstall` canonicaliza el orgId a 18 caracteres antes de llamar, con el comentario
           explicando por qué (el AuthBroker resuelve por id de 18; uno de 15 nunca resolvería).
           `findOrgEntry` acepta el prefijo de 15 al leer la respuesta porque el servicio devuelve
           la forma que tenga guardada — asimetría deliberada y correcta.
  S4 PASS  Esta corrida de QA no escribió en ninguna org. Las únicas llamadas a Salesforce fueron
           SELECT de solo lectura sobre InstalledSubscriberPackage en tres orgs de trainings.

SIN BLOQUEADORES DE SEGURIDAD.
