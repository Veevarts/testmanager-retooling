Tests encontrados en PR Veevarts/MigrationToolETL #178 — IM-1220
=================================================================
Archivo: src/data-migration/infrastructure/destination/sql-server/query-regression/visitor-management-query.spec.ts
Estado: NUEVO en el PR (+394 líneas), 24 assertions.
Tipo: estático — parsea visitor_management.sql y los 3 children con text anchors
      (no requiere DB ni testcontainers; corre en 0.2s).

Grupos de assertions (del run en PR HEAD 85e0145):
1. "arm 1 mirrors ticketing_only_program.sql" — expresión de lookup, gate por ticket line,
   unwindowed exactamente mientras el child lo sea (tripwire del dead window IM-791).
2. "arm 2 mirrors ticketing_only_event.sql" — expresión verbatim del child, gate sobre
   event-registration lines, misma window del child, y assertion NEGATIVA: arm 2 nunca
   selecciona la window expression (EarliestEventRegistrationDate) como lookup.
3. "arm 3 mirrors ticketing_direct_registrant.sql" — CTE chain byte-idéntica al child,
   fecha = EVENT.STARTDATE del registrant, misma window.
4. "date filter wiring is preserved" — bindea los parámetros del extractor igual que sus
   siblings (@hasDateFilterFrom/@dateFilterFrom/@hasDateFilterTo/@dateFilterTo).
5. "staged extraction stays a self-contained artifact" — SET NOCOUNT ON; cada #Vm* dropped
   antes de usarse y al final (re-runnable); índice en cada join key; WRITE AUDIT: todo
   INTO/DROP TABLE/CREATE..ON apunta a #Vm* y ningún DML verb aparece; eligibility chain
   fuera del statement final.
6. "pre-aggregation stays equivalent to the children's TOP 1" — MIN sobre CAST date por
   sales order, restringido a STARTDATE no-null.

Resultado: 24/24 PASS (ver test-run-log.txt).
Suite completo query-regression: 620 passed / 169 skipped (docker-gated) / 0 failed
(ver ../scenario-5/gates-log.txt).
