=== docs/altru-model-context.md diff ===
diff --git a/docs/altru-model-context.md b/docs/altru-model-context.md
index 6820434..f60f3a2 100644
--- a/docs/altru-model-context.md
+++ b/docs/altru-model-context.md
@@ -381,6 +381,9 @@ graph TD
 - IM-916: Exclude direct registrants when current attendance already has the same `EVENTID + CONSTITUENTID`, or when a conservative same-event/same-constituent sale-order ticket/event-registration candidate exists without a formal `REGISTRANT.ID` link.
 - IM-916: Exclude `REGISTRANT.CONSTITUENTID IS NULL`, `ISCANCELLED = 1`, and `WILLNOTATTEND = 1` rows from ticket/attendance migration unless a later approved anonymous-attendee strategy exists.
 - IM-916: UI type is represented by `REGISTRANT.ISWALKIN` (`0` preregistration-style, `1` walk-in); status flags are separate (`ISCANCELLED`, `WILLNOTATTEND`, `ATTENDED`). Attended direct rows should count as confirmed attendance, while other active eligible rows count as sold/registered capacity.
+- IM-1220: Visitor Management is a parent-of-lookup, not an independent object. Its `Implementation_External_ID__c` is the ISO `yyyy-mm-dd` string that every Ticketing export resolves through `Auctifera__Visitor_Management__c`, so its date set must cover every date the ticketing queries can derive. Its lineage therefore spans `SALESORDER`, `RESERVATION`, `SALESORDERITEM`, `SALESORDERITEMTICKET`, `SALESORDERITEMEVENTREGISTRATION`, `SALESORDERITEMEVENTREGISTRANTREGISTRATION`, `EVENTPRICE`, `EVENT`, `REGISTRANT`, `REGISTRANTREGISTRATION`, `SALESORDERITEMTICKETREGISTRANT`, `TICKET` and `CONSTITUENT` — not just the ticket tables it used to read.
+- IM-1220: The ticketing date a row is *filtered* on is not always the date it *points at*. In `ticketing_only_event.sql` the `Auctifera__Visitor_Management__c` lookup is `COALESCE(RESERVATION.ARRIVALDATE, earliest ticket EVENT.STARTDATE, SALESORDER.TRANSACTIONDATE)` — the same expression as `ticketing_only_program.sql` — while `Ticket_Date_Do_Not_Map` *and that file's row window* use the earliest event-registration `EVENT.STARTDATE` instead. Read the column alias, never the nearest COALESCE.
+- IM-1220: `RESERVATION.ARRIVALDATE` outranks both the ticket event date and the transaction date wherever a ticketing row carries a reservation. Group reservations are routinely booked years ahead of the transaction that pays for them, so a query that derives a visit date without `ARRIVALDATE` silently produces the booking date. Measured on Long Island Children's Museum: 165 ticketing rows over 136 distinct arrival dates.
 
 ## Donation, pledge, and matching-gift rules learned
 
@@ -694,6 +697,7 @@ graph TD
   construction. Do not re-union the per-role date-range tables (drops system roles)
   and do not export inactive-definition rows (retired segments / tenant-disabled
   roles).
+- **Visitor Management (IM-1220):** one row per distinct calendar date referenced by any Ticketing export, emitted as the `UNION` of the three ticketing queries' lookup expressions — `ticketing_only_program.sql` and `ticketing_only_event.sql` (both `COALESCE(RESERVATION.ARRIVALDATE, earliest ticket EVENT.STARTDATE, SALESORDER.TRANSACTIONDATE)`, differing only in population gate and window) and `ticketing_direct_registrant.sql` (the eligible registrant's `EVENT.STARTDATE`). Change any of those three and you must change the parent in the same commit: a date a child derives and the parent does not is a dangling `Auctifera__Visitor_Management__c` that an operator has to add by hand. Superset is the safe direction — a surplus date record is inert, a missing one breaks the lookup.
 - **Donation Opportunity:** eligible payment/installment key `COALESCE(RECURRINGGIFTINSTALLMENT.ID, FINANCIALTRANSACTION.ID)` after line aggregation. Do not emit one opportunity per donation line.
 - **Shared donation eligibility gate (IM-1193):** five queries decide from the same predicate which donation transactions exist — `donation_transaction.sql` (the Opportunity), `fund_assignment_donations.sql` (its fund assignments), `npsp_partial_soft_credit.sql` and `npsp_account_soft_credit.sql` (which Opportunities exist), and `pos-purchase/sales_order_only_donation.sql` (the POS Purchase those rows reference). Change one and you must change all five: a file left behind does not merely go stale, it produces a dangling reference — an Opportunity whose `vnfp__POS_Purchase__c` points at a POS Purchase that was never exported.
 - **Donation Fund Assignment:** qualifying donation line item / designation. Do not attach membership or pledge source lines as donations. For an in-place adjustment (IM-1193) the emitting line is the *replacement* `Standard` line, not the superseded original: the superseded line and its `Reversal` cancel out, so emitting both Standard lines doubles the fund assignment for one gift (rev-10059002, two $5,000 assignments on one $5,000 opportunity). The replacement's fund/application come from its own `REVENUESPLIT_EXT` row when it has one and are inherited from the superseded line otherwise.
