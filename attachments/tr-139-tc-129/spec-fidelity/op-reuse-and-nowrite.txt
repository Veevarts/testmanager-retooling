== files changed base(8d66dba6)..merge(adcef7d2) ==
src/implementations/infrastructure/seeds/specs/ic-3-28-fiscal-year.json
src/implementations/infrastructure/seeds/specs/ic-3-28-fiscal-year.spec.ts

op-reuse: adapter/domain/handler changed by #252? (expect none)

NO-WRITE proof — DML / write patterns over Organization in the step-2 apex:
  DML statements: NONE (pure read)
  SOQL SELECT: ['Organization org = [SELECT FiscalYearStartMonth, UsesStartDateAsFiscalYearName FROM Organization LIMIT 1];']
  lines containing FiscalYearStartMonth= : 1 (all inside System.debug string concatenation):
     System.debug(LoggingLevel.INFO, '[3.28] INFORMATIONAL fiscal year read-back: Org
  throw statements: ['// attest. This step contains NO throw — it can never false-fail on an org whose', '// Best-effort parse of the intended start month. NEVER throws — a blank or', '// INFORMATIONAL — this step ALWAYS succeeds (no throw anywhere), so it can NEVER']
