Probar que el harness PUEDE fallar — no basta con que este verde.

M1  revertir el arreglo del PR 390
    src/csm/tasks/domain/task-state.ts
      -  Done: ['New', 'In_progress', 'Rejected'],
      +  Done: ['New', 'Rejected'],

    Resultado: el harness se cae con
      ConflictException: Invalid task transition: Done -> In_progress.
                         Allowed from Done: [New, Rejected].   (409)

    Es exactamente el defecto que el product owner encontro en dev: el tablero
    ofrecia el arrastre y la maquina lo rechazaba. El check "done -> doing applies,
    and CLEARS resolved_at (the bug the PO hit)" muere con la mutacion, asi que el
    347/347 de la corrida limpia no es un verde vacio.

    Log: mutation-M1-revert-390.log
    Restaurado: si (git status limpio en ese archivo despues de la prueba).
