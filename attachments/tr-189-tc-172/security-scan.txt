=== 1. Secretos / credenciales en el diff 1ad3b4ce..ceae03b1
+Use `userCreatedDate` for Salesforce `User.CreatedDate`. `userCreatedAt`, `accountCreatedDate`, and `accountCreatedAt` remain accepted legacy aliases; user aliases take precedence when both are present. For `specific_users` surveys, a configured token must match either the `respondentId` or `respondentEmail` passed by the embedding app; both comparisons are case-insensitive. General NPS delivery still requires a non-empty `respondentId`, so a matching email does not replace that required delivery context. `respondentName`, `respondentEmail`, `respondentPhone`, and `respondentTitle` are optional snapshots persisted with a new response; they are display context and must not be treated as proof of identity.
+  splitAudienceTokens,
+  const organizationIds = splitAudienceTokens(audienceDetails);
+import { detailsTokens, parseSurveyRow, toPublicSurvey, toSurveyDeliveryContext } from "../lib/surveyRuntime.js";
(sin coincidencias arriba = limpio)

=== 2. Concatenacion de entrada en consultas
+    const updateRes = await fetch(`${baseUrl}/api/admin/surveys/${survey.id}`, {
+    const staleScopeUpdateRes = await fetch(`${baseUrl}/api/admin/surveys/${survey.id}`, {
(sin coincidencias arriba = limpio)

=== 3. Cambios en dependencias

=== 4. PII en logs nuevos
