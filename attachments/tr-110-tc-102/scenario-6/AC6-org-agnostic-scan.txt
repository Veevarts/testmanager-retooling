AC6 verify — ic-5-23-campaigns-page-layout.json (v3 on main)
======================================================================

Spec: version=3 · feasibility=hybrid · steps=3

VERDICT: PASS — spec is org-agnostic and record-type-agnostic.

Checks:
  1. Hardcoded layout fullName in inputPayload: 0 hits
     (Regex matches in description text: 0 — allowed as example prose)
  2. 'Campaign.Default' in inputPayload of any step: 0 hits
     (In descriptions as explanatory text: 2 — allowed)
  3. Step 1 inputPayload.layout = '{{campaignLayout}}' → template placeholder ✓
  4. Step 1 inputPayload has 'recordType' key: False → correctly omitted

Additional structural evidence:
  - Step 1 requiredInputs[0].discovery.kind = 'layouts'
  - Step 1 requiredInputs[0].discovery.object = 'Campaign'
  - Steps kinds: ['AUTOMATED', 'MANUAL', 'MANUAL']

Conclusion:
  AC6 PASS: no hardcoded layout fullName in inputPayload; no recordType key;
  layout resolved at runtime via live-discovered select. Case is org-agnostic
  (works with vnfp / Auctifera / any namespace) and record-type-agnostic (works
  whether Campaign has 0 or N custom RTs).
