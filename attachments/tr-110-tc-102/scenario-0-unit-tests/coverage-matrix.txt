Unit test coverage — PR Veevarts/MigrationAppBackend#250
=========================================================

PR touches ONLY one JSON spec file:
  src/implementations/infrastructure/seeds/specs/ic-5-23-campaigns-page-layout.json

Existing structural gate that covers this change:
  src/implementations/infrastructure/seeds/specs/ic-seeds.spec.ts
  - bulk parseSpec validation on every *.json spec in the folder
  - dev PR body confirms 79/79 pass with the v3 spec
  - tsc --noEmit clean · eslint clean

What ic-seeds.spec.ts proves (structural gate):
  - v3 JSON parses via parseSpec (kind, toolName, stepId, feasibility, category, order)
  - each AUTOMATED step has a toolName
  - each MANUAL step has inputPayload.guide
  - no schema violations

What it does NOT prove (behavioral):
  - T2 assign op actually re-points every layout assignment when recordType is omitted
  - discovery kind 'layouts' returns the correct list from a live org
  - Profile read-back verification succeeds after assign
  - manual step guide rendering in MigrationApp UI

AC ↔ test coverage
------------------
| AC   | Structural (parseSpec) | Behavioral                                                 | Verdict |
|------|-----------------------|------------------------------------------------------------|---------|
| AC1  | ✔ covered              | Requires MigrationApp UI running against seeded catalog    | not-run (seed pending) |
| AC2  | ✔ covered              | Requires T2 execution + Profile read-back                   | not-run (seed pending) |
| AC3  | ✔ covered              | Dev-verified live on CamiloDevOrg (46 profiles, 42 assigned, verified=true) | PASS (dev evidence) |
| AC4  | ✔ covered              | Requires MigrationApp UI showing guide rendering            | not-run (seed pending) |
| AC5  | ✔ covered              | Requires MigrationApp UI showing guide rendering            | not-run (seed pending) |
| AC6  | ✔ covered              | Static JSON grep (this TR runs it locally)                 | PASS   |

Gap conclusion
--------------
Dev verified end-to-end on CamiloDevOrg (org sin custom RTs). illinois-trainings
provides the orthogonal test topology (5 custom RTs), but MigrationApp UI can
only execute after the AWS SSO refreshes and `MigrationApp-dev` reseeds v3.

Runner check: cannot execute `npm test` here — this is the testmanager-retooling
QA repo, not MigrationAppBackend. Upstream CI gate on the PR merge is what
enforces the parseSpec assertion. Not blocking.
