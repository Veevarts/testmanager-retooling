PRE-run baseline — illinois-trainings @ 2026-07-16
==================================================

Campaign RecordTypes on illinois-trainings (5 custom, all active)
------------------------------------------------------------------
  Event                     · NS=(none)
  Fundraising               · NS=(none)
  General                   · NS=(none)
  Marketing                 · NS=(none)
  Volunteer_Campaign        · NS=(none)

  This is the OPPOSITE topology from CamiloDevOrg (which has 0 custom RTs).
  Great differential coverage for the "record-type-agnostic" claim:
  the T2 assign op must re-point 5 per-RT + 1 default = 6 layout assignments
  per profile without depending on any 'Campaign.Default' record type.

Campaign layouts discoverable on illinois-trainings (6 total)
-------------------------------------------------------------
  Campaign-Campaign Layout                              · ns=(none)
  Campaign-Campaign Layout                              · ns=vnfp
  Campaign-NPSP Campaign Layout                         · ns=npsp
  Campaign-Veevart Campaign Layout                      · ns=vnfp
  Campaign-Veevart Campaign Layout V2 - Aug 2022        · ns=vnfp
  Campaign-Veevart Campaign Layout V3 - Mar 2024        · ns=vnfp   ← latest, expected default post-run

Profile count: 20 profiles on illinois-trainings
  (much smaller than the mature-org Tucson case that hit the tabVisibilities
  write-back limitation flagged by dev)

ProfileLayout junction rows for Campaign-* layouts: 187 (via Tooling API)
  These are per-profile-per-RT assignments the T2 assign op will re-point.

Conclusion — case v3 SHOULD produce the following observable POST state:
  1. Every profile with access to Campaign gets its Campaign layout assignments
     (default + per-RT) re-pointed to 'Campaign-vnfp__Veevart Campaign Layout V3 - Mar 2024'
  2. Assign op succeeds (verified=true, success=true, assigned>0) with no
     reference to 'Campaign.Default'
  3. Manual step 2 (End Date reorder) presents guide + parks AWAITING_HANDOFF
  4. Manual step 3 (Lightning Record Page org-default) presents guide + parks
     AWAITING_HANDOFF; SKIP if 'Veevart Campaign Record Page' is absent

BLOCKED-ON: dev refreshes AWS SSO → reseed MigrationApp-dev with v3 →
IE runs plan against illinois-trainings from MigrationApp UI.
