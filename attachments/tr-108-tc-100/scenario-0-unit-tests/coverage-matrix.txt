Unit test coverage — PR #211 (MigrationAppBackend)
===================================================

Test file: src/implementations/infrastructure/seeds/specs/ic-seeds.spec.ts
Type: Bulk-glob parseSpec validation (structural, not behavioral).

What it proves:
  - ic-9-2-accounting-batches.json is a structurally valid spec
    (kind, toolName on AUTOMATED steps, stepId non-blank, category/order,
    inputPayload.apex present, inputPayload.guide for the MANUAL step).
  - Same guardrail for every other spec in the folder (~80 files).

What it does NOT prove:
  - Apex logic correctness (Auctifera.AutomaticBatchSchedule invocation,
    vnfp.AutomaticBatchSchedule invocation, CronTrigger registration).
  - Idempotency claim (AC5) at runtime.
  - "already scheduled" error swallowing.
  - Discovery of Donation RecordType at runtime (AC2 branch selection —
    managed vnfp preferred over unmanaged homónimo).
  - RECURSIVE / async batch cron creation (INFORMATIONAL crons that appear
    only after an async batch finish() runs).

AC ↔ test coverage
------------------
| AC   | Structural (parseSpec) | Behavioral | Gap                              |
|------|------------------------|------------|----------------------------------|
| AC1  | ✔ covered              | ✘          | Verified live-only on CamiloDevOrg |
| AC2  | ✔ covered              | ✘          | Verified live-only on CamiloDevOrg |
| AC3  | ✔ covered              | ✘          | Verified live-only on CamiloDevOrg |
| AC4  | ✔ covered (MANUAL)     | N/A        | No entry point; manual by design  |
| AC5  | ✘                      | ✘          | Idempotency uncovered by tests     |
| AC6  | ✔ (org-agnostic byproduct) | ✘      | Verified statically via JSON grep  |
| AC7  | ✘                      | ✘          | Regression-evidence uncovered      |

Gap conclusion
--------------
Dev verified the Apex end-to-end LIVE on CamiloDevOrg (Auctifera 8.291 /
vnfp 2.286) + Tucson im696-tucson (per IM-735 confidence report 2026-06-12).
No unit tests exist for anonymous Apex bodies (expected — they run inside
T6 tool, not the Node runtime). QA compensates by:
  - PRE-state SOQL baseline in illinois-trainings (this TR).
  - POST-state SOQL bundle handed to the IE to run after MigrationApp
    executes the plan in illinois-trainings.
  - AC6 verified statically via JSON grep (0 hardcoded Ids, 3 runtime
    discovery markers).

Runner check: cannot execute `npm test` here — this repo (testmanager-retooling)
is the QA repo; the tests live in MigrationAppBackend and are validated by
CI on that repo. PR body claims "ic-seeds.spec.ts passes (79/79); JSON-only
change." Not blocking — the CI gate is upstream.
