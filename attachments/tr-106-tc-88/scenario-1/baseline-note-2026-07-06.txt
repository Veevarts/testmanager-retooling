TC-88 is entirely UI/metadata-driven: page layouts, field sets, Lightning Record Pages, list view button layouts.

Automated observable baseline is NOT queryable via standard SOQL. Metadata API + Setup UI walkthrough are required.

Attestation for this TC will come from:
  1. BE PR #205 + PR #214 merged status (backend delivery).
  2. IM-735 (2026-06-12) delivery report: hybrid = verified on both orgs.
  3. IE screenshots after MigrationApp catalog run in illinois-trainings.
