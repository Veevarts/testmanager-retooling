diff --git a/src/implementations/infrastructure/seeds/specs/ic-3-25-setup-profiles-and-users.spec.ts b/src/implementations/infrastructure/seeds/specs/ic-3-25-setup-profiles-and-users.spec.ts
index 22ab7aca..91a046a2 100644
--- a/src/implementations/infrastructure/seeds/specs/ic-3-25-setup-profiles-and-users.spec.ts
+++ b/src/implementations/infrastructure/seeds/specs/ic-3-25-setup-profiles-and-users.spec.ts
@@ -14,8 +14,10 @@ import { resolve } from 'node:path';
  *
  * Pins:
  *  - version bump + case/template lockstep (v2);
- *  - feasibility flipped manual → hybrid (a regression back to manual would
- *    drop the case from WORKING_CASE_IDS and lose the automation);
+ *  - feasibility flipped manual → hybrid (the master template includes this
+ *    case either way via predicate selection — every non-manual case plus the
+ *    manual tier, minus EXCLUDE_FROM_MASTER — so it can no longer silently drop;
+ *    a regression back to manual would instead lose the AUTOMATION pinned below);
  *  - the two AUTOMATED T3 `settings` steps carry the exact org-agnostic field
  *    values (a changed field name / value silently ships the wrong toggle);
  *  - the two client-specific parts remain MANUAL handoffs.
diff --git a/src/implementations/infrastructure/seeds/specs/ic-4-05-transaction-journal-layouts.spec.ts b/src/implementations/infrastructure/seeds/specs/ic-4-05-transaction-journal-layouts.spec.ts
index 0ec96ac6..51d02d6f 100644
--- a/src/implementations/infrastructure/seeds/specs/ic-4-05-transaction-journal-layouts.spec.ts
+++ b/src/implementations/infrastructure/seeds/specs/ic-4-05-transaction-journal-layouts.spec.ts
@@ -4,9 +4,11 @@ import { resolve } from 'node:path';
 
 /**
  * Lockstep test for the case-4.05 seed JSON (Update Transaction Journal &
- * Journal Page Layouts). 4.05 is a master/`auto` case bundled in
- * WORKING_CASE_IDS, so it needs the same structural pin its siblings have
- * (ic-4-01, the two demos). Locks the behavioral contract the Implementations
+ * Journal Page Layouts). 4.05 is an `auto` case, so the master template includes
+ * it automatically via predicate selection (every non-manual case minus
+ * EXCLUDE_FROM_MASTER) — no allowlist pin needed, and the catalog-completeness
+ * test in `master-template-composition.spec.ts` fails CI if it ever drops out.
+ * Locks the behavioral contract the Implementations
  * handoff depends on and the v3 surface this case introduced:
  *  - 3 ordered steps T6 (accounting settings) -> T2 (layout assign) -> T6
  *    (legacy-field guard);
