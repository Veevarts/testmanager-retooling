safesql aggregate wrapper — DEFERRED (bastion outage this session).

Bastion 98.95.60.135:22 (bastion.internal.veevartapp.com) is down:
  ssh -i <key> ec2-user@bastion.internal.veevartapp.com
  → "Connection timed out during banner exchange" (T+10s ConnectTimeout).

All 4 profiles (tucson, tucson_backup3, illinois_2, aspen) fail with
"Tunnel did not come up on port 4143X" for the same reason.

Note: earlier in this same session (TR-169, ~90 min ago) tucson bastion WAS reachable and
returned 19,617 rows for IM-1204. The outage started sometime in the last hour — matches
the recurring intermittent bastion outages Camilo documented in IM-1192.

Wrapper query is prepared and would run instantly when bastion returns
(attachments/tr-170-tc-153/scenario-1/aggregate-wrapper.sql). It reuses the CTEs from the
PR head (MembershipLineItems, ResolvedMembershipFinancialTransactions, RecurringInstallments)
and emits a numeric aggregate:
  - total_membership_lines
  - lines_with_own_ft / lines_missing_own_ft (grain check)
  - delta_line_items: rgi.InstallmentDate IS NULL AND own_ft.CALCULATEDDATE <> rmft.FinancialTransactionDate
  - delta_month_or_year_jump / delta_year_jump
  - recurring_would_have_changed_but_installment_wins (should be 0 — IM-904 preservado)
  - recurring_rows_total
  - lines_where_own_ft_is_refund_and_ranked_child_differs

Expected Tucson (dev-reported line-item grain): 17 delta_line_items. If wrapper runs later,
it must match this number byte-a-byte to confirm the fix mechanics.

Substituted with:
  - Diff-level review (surgical, verifiable): the fix inserts own_ft.CALCULATEDDATE at
    position #2 in COALESCE (after InstallmentDate, before rmft.FinancialTransactionDate).
    Contract preserved.
  - 5/34 IM-1205 static specs FAIL when revert applied (revert-proof).
  - Dev PR body numbers: 97,014 rows LI PRE=POST, 33 dates change, 10 month-jump, 2 year-jump,
    0 recurring affected in any profile, 41/41 live-database tests PASS.
