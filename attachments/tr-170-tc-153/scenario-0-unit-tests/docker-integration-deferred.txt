Docker integration suite — DEFERRED (same colima startup-race issue that TR-169/TC-152 documented).

Local this session: 41/41 FAIL, all with "Log stream ended and message /.*Started.*/ was not received"
even after patching the spec with `.withStartupTimeout(180000)`. The mssql-server 2022 container
under colima/x86_64 needs ~90s to boot and the log stream disconnects earlier under system load
(colima has ptt-postgres + prior test containers running concurrently). Root cause is local
environment resource contention, NOT a defect in the PR.

Substituted with:
  - Full jest static regression: 558/558 PASS on PR branch (matches dev claim exactly).
  - fund-assignment-memberships + donation-split-receipting spec files: 34/34 PASS + 41 skipped
    (the 41 are the docker-gated tests dev ran).
  - safesql aggregate wrapper on tucson (see scenario-1).
  - Revert-proof at static-spec level (see scenario-0-unit-tests/revert-proof.txt).

Dev-reported RUN_DOCKER_SQL_QUERY_TESTS=1 result on their machine, per PR #172 body:
  - 41/41 live-database tests PASS.
  - lint + 558 unit tests + build all pass.
  - Every guard checked by deliberately re-breaking the query 4 different ways; tests fail
    each time.

This mirrors the same pattern noted in memory reference-mtoolet-qa-execution-gotchas: colima
startup > 60s requires .withStartupTimeout override AND enough system resources; dev-side
validation is the primary evidence when local env cannot meet both.
