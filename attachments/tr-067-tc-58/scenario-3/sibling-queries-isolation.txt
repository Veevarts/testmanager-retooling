Scenario 3 — Refund FAs / sibling FA queries unchanged
=======================================================

Method 1: Static — git diff main...pr-129 on sibling SQL files
  Files inspected:
    - src/.../queries/fund-assignment/fund_assignment_donations.sql
    - src/.../queries/fund-assignment/fund_assignment_payments.sql
  Diff: EMPTY (zero bytes changed)

Method 2: Full PR change-set name list
  $ git diff --name-only main...pr-129 | grep -v openspec | grep -v docs
    src/data-migration/infrastructure/destination/sql-server/queries/fund-assignment/fund_assignment_memberships.sql
    src/data-migration/infrastructure/destination/sql-server/query-regression/fund-assignment-memberships-query.spec.ts

  Conclusion: only the membership FA query + its regression spec changed.
  Donations and payments FA queries (including refund FA logic embedded inside them) are byte-identical between baseline and post-fix branches.

VERDICT: PASS
- Sibling queries cannot regress because their SQL is unchanged
- Refund FAs (handled within these sibling queries) cannot change posted_date because the SQL projecting that column was not touched
- Static proof sufficient; no data-level verification required when SQL is unchanged

NOTE on the original ticket wording:
  The bundle's Scenario 3 talks about a separate "refund FA query" but in practice refund FA emission lives inside fund_assignment_donations.sql / payments.sql, not in a separate file. Verdict remains PASS via byte-identical SQL on the sibling files.
