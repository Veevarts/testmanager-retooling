TC-180 — TESTS ANADIDOS O MODIFICADOS POR EL PR #186

  src/.../query-regression/matching-gift-query-chain.integration.spec.ts   (contenedor)
    - "donation_update_matching_gift resolves every donor to the Opportunity its own export emits"
      4 pares esperados: cuota de recurring gift, pledge, transaccion de membresia, ingreso de la orden
      3 aserciones not.toContain: las transacciones pagadoras NO se emiten (serian stubs)
    - "donation_update_matching_gift drops a membership whose OTHER anchor disqualifies it"
      MG6: linea de membresia de una orden pendiente, con el donante como hijo de pago
      MG7: la transaccion propietaria lleva cuota de recurring gift
    - "donation_update_matching_gift drops a link whose matching-gift claim the donations
      export never emits"  (MG5, el claim de $0)

  src/.../query-regression/matching-gift-query.spec.ts                     (unitario)
    - "resolves every donor shape to an emitted Opportunity"   (una asercion por rank)
    - "judges the membership gate on every anchor, not on the donor transaction"
      incluye un not.toMatch para que el gate no vuelva a juzgarse sobre el donante
    - "keeps only candidates an Opportunity export emits"
    - invariante de una sola evaluacion del gate (una definicion, un uso, sin EXISTS)
    - comparacion del preludio completo contra donation_transaction.sql

  src/.../query-regression/sql-server-query-fixture.ts                     (fixture compartida)

EJECUCION EN LA RAMA DEL PR
  npm run test:queries          651 passed / 828 total (177 skipped = los de contenedor)  EXIT 0
  npm run test:queries:docker   175 passed / 175 total, 16 suites, 724,6 s                EXIT 0

  Ningun fallo. Ver test-run-log.txt e integration-docker-PASS.log.
