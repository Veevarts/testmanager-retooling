IM-1086 — 5 upgrades spec-fidelity (independent parse) + 7 stay-manual review
=============================================================================
5 UPGRADES @ a2ee3cbf (each case.version=2 + template.version=2, lockstep asserted):
  IC 1   session-settings-cdn  v2 auto   step1 T3 op=settings (REUSED)  LightningExperienceSettings.enableAuraCDNPref=true       -> AC IC 1
  IC 5.2 internal-notification v2 hybrid step1 T3 op=workflowAlert (REUSED) OWEA sender + ADD recipient; step2 MANUAL remove 'Account owner' -> AC IC 5.2
  IC 3.29 help-menu           v2 auto   step1 T3 op=customHelpMenuSection (NEW) 'Veevart Resources' + 3 fixed links               -> AC IC 3.29
  IC 6.04 default-location    v2 auto   step1 T3 op=translations (NEW) en_US override Auctifera__Default_Location = {{museumAddress}} -> AC IC 6.04
  IC 6.05 pos-email-labels    v2 hybrid step1 T3 op=translations (NEW) 5 POS/ticketing labels {{museumName}} + fixed Exchange; step2 MANUAL Default Workflow User -> AC IC 6.05
=> parsed independently; matches the 5 Gherkin ACs exactly. workflowAlert ADD-only (remove is manual), translations set-only.

7 STAY-MANUAL — QA soundness review (rationale defensible? each names a concrete blocker):
  5.05 Default Ack Status ..... SOUND (strongest). GROUND-TRUTH: after a *successful* RecordType deploy setting the managed
                                 npsp__Acknowledgment_Status__c default, a NEW Donation Opp inserts null (proven via anon Apex
                                 insert→requery→delete). A standard Type default persisted; the managed one did not → managed
                                 picklist RT-default is silently dropped by the platform. Correct refusal (not automatable safely).
  3.31 Queueable Delay ........ SOUND. ApexSettings metadata type has NO enqueue-delay field (full retrieve). No API surface.
  3.18 Home Page Dashboards ... SOUND. Per-app FlexiPage home pages + App-Default assignment + Dashboard components (Reports-Package dep). FlexiPage precedent.
  3.27 Museum Days Off ........ SOUND. MuseumDaysOff component on the Company Info FlexiPage + days-off data. FlexiPage precedent.
  5.19 Ack Letters ............ SOUND. Per-client letter CONTENT + signature image upload/URL. Inherently manual.
  5.04 Opportunity Stages ..... SOUND (deferred). Needs a NEW businessProcess/salesProcess op + record-type assignment — out of scope this spike → follow-up ticket.
  6.01 Override Field Labels .. SOUND (deferred). Translation Workbench en_US feasible but needs a distinct CustomObjectTranslation op + managed field-API-name resolution across 5 objects → follow-up ticket.
=> all 7 defensible: no API surface / proven managed silent-drop / FlexiPage+content dependency / deferred new-op requirement.
   The deferred 5.04/6.01 are correctly scoped as follow-ups (they need ops this spike did not build). Live probes were dev-only; QA validated plausibility + catalog fit.

MASTER-INCLUSION open question (ticket asks whether to pin the 6 flipped cases into WORKING_CASE_IDS):
  Answered from IM-1087/TR-136 investigation: IM-1090/#262 rewrote the master seed to PREDICATE-BASED selection (every
  auto/hybrid case always included; EXCLUDE_FROM_MASTER = only ic-veevart-auth). So on current main the flipped IC 1/3.29/6.04
  (auto) + 5.2/6.05 (hybrid) enter the master by the predicate — no WORKING_CASE_IDS pin needed. Resolved by IM-1090; no action.
