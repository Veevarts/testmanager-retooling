Scenario 5 — Dev tests (donation-record-type-query.spec.ts)
============================================================
Runner: npx jest donation-record-type-query --runInBand

Resultado: 8/8 tests PASS

Tests:
  1. derives in-kind gifts from payment method detail without joining raw payment rows into EligibleLines
  2. aggregates the in-kind signal at transaction grain before selecting the record type
  3. derives matching gift record type from active REVENUEMATCHINGGIFT claim status at transaction grain
  4. preserves stock gifts as their own configurable donation record type slot
  5. preserves property gifts as their own configurable donation record type slot
  6. preserves planned gift as its own configurable donation record type slot
  7. derives pledge in-kind record type at pledge grain from gift-in-kind payment evidence
  8. documents required Salesforce record type environment variables

VERDICT: PASS
- Coverage por cada slot agregado (Matching, Stock, Property, Planned, Pledge In-Kind)
- In-Kind tiene 2 specs específicos (signal extraction + grain-safe aggregation)
- 1 spec dedicado a documentar las env vars requeridas (canary para cuando se agregue otro slot sin updating env vars)
- Sin cross-type spec específico, pero la precedencia se valida estáticamente por el orden del CASE
