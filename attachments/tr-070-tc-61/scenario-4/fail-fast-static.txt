Scenario 4 — Fail-fast on missing env var
==========================================
Method: inspección estática del extractor donation-transaction.extractor.ts (mismo patrón en donation-pledges.extractor.ts)

Código verificado:
  private getRequiredEnv(name: EnvVariable): string {
    const value = process.env[name];
    if (!value) {
      throw new Error(`Missing required environment variable: ${name}`);
    }
    return value;
  }

Y la sequencia de calls al inicio del replaceTokens:
  - SF_DONATION_RECORD_TYPE_ID
  - SF_IN_KIND_GIFT_RECORD_TYPE_ID
  - SF_MATCHING_GIFT_RECORD_TYPE_ID
  - SF_PLANNED_GIFT_RECORD_TYPE_ID
  - SF_STOCK_GIFT_RECORD_TYPE_ID
  - SF_PROPERTY_GIFT_RECORD_TYPE_ID

Cualquier env var ausente → throws Error con el nombre exacto ANTES de cualquier query, write a CSV, o write a SF.

VERDICT: PASS
- El mensaje de error indica exactamente cuál env var falta (interpolación de `${name}`)
- El fail-fast es estricto (NO defaults silenciosos, NO empty strings)
- El patrón es consistente con el existente de IM-767 (mismo getRequiredEnv helper)
- El dev test "documents required Salesforce record type environment variables" valida que la lista es completa
