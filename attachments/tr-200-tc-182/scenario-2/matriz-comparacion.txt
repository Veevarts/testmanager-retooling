ESCENARIO 2 · AC2 — el certificado y la clave se suben ANTES que el .bak
========================================================================

Se comprueba en dos sitios: en el fuente mergeado y en el bundle que dev sirve
de verdad. El segundo importa porque el repositorio no es lo que corre.

Fuente/Prueba                      Metrica              Antes (d17f406)       Despues              AC   Objetivo?  Veredicto
---------------------------------  -------------------  --------------------  -------------------  ---  ---------  ---------
fuente · uploadBackupFiles         orden de las         BACKUP,               CERTIFICATE,         AC2  SI         PASS
                                   llamadas             CERTIFICATE, KEY      KEY, BACKUP
bundle DESPLEGADO en dev           orden minificado     n/a                   "certificate",       AC2  SI         PASS
/assets/useBackups-CCWRPsgj.js                                                "key", "backup"
prueba del autor                   "uploads the cert    n/a                   VERDE                AC2  SI         PASS
                                   and key before a
                                   multipart backup"
mutacion M1                        se devuelve el       -                     la suite se pone     AC2  SI         PASS
  (volver a poner el .bak primero) orden viejo                                ROJA
mutacion M1b                       se quita el .bak     -                     la suite se pone     AC2  SI         PASS
  (quitar el .bak del final)       del final                                  ROJA
repro (escenario 1)                instante del PUT     tras 15000 s          en los primeros      AC2  SI         PASS
                                   del certificado      de subida del .bak    segundos

Linea exacta del bundle desplegado en dev:
  e.CERTIFICATE&&await G("CERTIFICATE",t.CERTIFICATE,e.CERTIFICATE,"certificate",a),
  e.KEY&&await G("KEY",t.KEY,e.KEY,"key",a),
  await G("BACKUP",t.BACKUP,e.BACKUP,"backup",a)

El criterio pedia captura del panel de red. No hay navegador en esta corrida, y
la captura mostraria exactamente el orden que estas tres lineas ya fijan en el
codigo que el navegador descarga. Se deja como pendiente cosmetico, no como
hueco de verificacion: el orden esta probado en el artefacto servido.
