PRUEBAS QUE TRAEN LOS PR · IM-1297
===================================

BACKEND · "PR #393" (37afb12d)
  src/backups/application/use-cases/sign-backup-upload-url.use-case.spec.ts   NUEVO   14 pruebas
  src/backups/infrastructure/entrypoints/api/backups.controller.spec.ts       tocado  +1 prueba
  -> npx jest (los dos ficheros): 22/22 PASS

FRONTEND · "PR #205" (257c71c)
  src/features/migration/api/backupUploads.test.ts   +12 pruebas en un describe nuevo
                                                     "backup single-PUT uploads (IM-1297)"
  src/features/migration/api/backups.test.ts         +2 pruebas
  -> npx vitest run (los dos ficheros): 34/34 PASS

TOTAL ANADIDO POR LOS PR: 29 pruebas nuevas, todas verdes.

CALIDAD DE LO QUE PRUEBAN — lectura, no conteo
-----------------------------------------------
Lo bueno: las guardas del backend estan probadas una por una, con it.each sobre
los tres estados de borrado y sobre tres formas de clave ajena. El frontend
separa con precision "negativa" de "ausencia" y prueba las dos ramas. Y hay un
detalle que se agradece: la prueba "rejects a backup that does not exist with
400, never 404" lleva el motivo escrito encima, asi que el que la cambie sabra
que esta rompiendo algo deliberado.

Lo que falta, y por eso QA anade cosas propias:
  G1 · AC1 no tiene NINGUNA prueba. Ninguna de las 29 reproduce la caducidad,
       que es el defecto del ticket. Todas prueban piezas del arreglo.
  G2 · Nadie ha mostrado que estas 29 puedan ponerse ROJAS por el motivo que
       dicen guardar. -> mutaciones.txt
  G3 · Ninguna mira si la ruta esta viva en ningun entorno, que con el fallback
       de 404 es justo lo que no se puede omitir. -> scenario-6-despliegue/
  G4 · La guarda de clave se probo con 4 claves ajenas obvias; faltan las formas
       que alguna capa podria normalizar de vuelta. -> scenario-4/
