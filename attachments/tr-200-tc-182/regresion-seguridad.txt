SEGURIDAD · barrido sobre lo ANADIDO por los dos PR
======================================================================
lineas anadidas revisadas: 832  (backend 383 · frontend 449)

[1. Secretos asignados en el codigo]
    LIMPIO — ninguna linea anadida asigna una credencial literal.

[2. Material criptografico embebido]
    LIMPIO — no hay claves ni certificados embebidos.

[3. Ejecucion de comandos / eval]
    LIMPIO — no se ejecuta ningun comando ni se evalua codigo.

[4. Concatenacion de consultas]
    LIMPIO — no hay construccion dinamica de consultas.

[5. PII / datos de cliente]
    LIMPIO — los unicos datos en las pruebas son ficticios (example.com, s3.upload, client-1).

[6. La URL prefirmada acaba en el log?]
    campos registrados: ['event', 'clientId', 'backupId', 'key', 'uploadStatus']
    LIMPIO — se registran clientId, backupId, key y uploadStatus; la URL firmada NO.

[7. Dependencias nuevas]
    be: ninguna — ni package.json ni el lockfile cambian
    fe: ninguna — ni package.json ni el lockfile cambian
