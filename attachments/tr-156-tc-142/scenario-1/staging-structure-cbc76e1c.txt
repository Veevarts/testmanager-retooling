TR-156 · IM-1161 / TC-142 · Escenario 1 — estructura y contrato de salida en cbc76e1c
(re-verificado; el .sql no cambió respecto de bebdc71, pero se re-mide porque la
corrida debe sostenerse con su propia evidencia)

  staged `SELECT … INTO #temp`        : 15
  `CREATE NONCLUSTERED INDEX`         : 15
  emparejamiento INTO <-> INDEX 1:1   : SÍ
  `DROP TABLE IF EXISTS`              : 30
  LEAD == TRAIL == los 15 staged      : SÍ (15/15)
  temp tables GLOBALES (##)           : ninguna
  `SET NOCOUNT ON`                    : presente
  contiene `GO`                       : NO
  DDL sobre objeto no-temp            : ninguno

CONTRATO DE SALIDA vs merge-base a8380f9a:
  ramas de 35 columnas — merge-base: 2 · cbc76e1c: 2
  rama A idéntica: SÍ
  rama B idéntica: SÍ
  las dos ramas coinciden entre sí: SÍ

  => 35 columnas, un único result set, sin cambios de columna/orden. El extractor
     `executeQueryAsStream` sigue sin necesitar modificación.

ESCENARIO 1: PASS (sin cambios respecto de TR-155)
