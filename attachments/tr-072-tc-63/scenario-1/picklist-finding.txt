DEFECT EVIDENCE — Role value not in target picklist (IM-919 / PR #134)

AC1 requires: export includes a 'Buyer' role row for the resolved primary contact (IsPrimary=true).
PR #134 emits: 'Donor' AS Role  (dev apply-progress.md: "role-label revalidation after switching Buyer to Donor").
Mapper (contact-role.mapper.ts) passes row.Role verbatim -> OpportunityContactRole.Role.

Target picklist OpportunityContactRole.Role in Tucson trainings (sf alias veevartd, admin.veevart@tucsonbotanical.org.trainings):
  Business User, Decision Maker, Economic Buyer, Economic Decision Maker,
  Evaluator, Executive Sponsor, Influencer, Technical Buyer, Other, Member

=> Neither 'Donor' nor 'Buyer' is a valid value. Only 'Member' (existing role) is valid.
=> The new resolved-primary-contact roles will be REJECTED on load (restricted picklist) or land blank.

Caveat: verified against the TRAININGS sandbox; must be reconfirmed against the actual Tucson TARGET org picklist before load. Either way the AC ('Buyer') and the implementation ('Donor') diverge and neither matches the current picklist.

Required resolution:
1. Decide the correct, picklist-valid Role value for the buyer/giver on membership Opportunities in the Tucson target org (add the picklist value, or use an existing valid one).
2. Align contact_roles.sql AND the IM-919 acceptance criteria to that value.
