TR-172 · TC-155 (IM-1212) · OWASP Top 10 (2021) applicability scan

Change scope: SQL query fix + 8-file rename of a suppression flag column name. No new endpoints,
no auth changes, no dependencies, no infra. Only new source-column reads (INSTALLMENT.DATE via
new JOIN on FK → PK).

### A01 · Broken Access Control — N/A
### A02 · Cryptographic Failures — N/A

### A03 · Injection — Applicable, PASS
Diff has no new user-input params. 3 new JOINs on primary key. WHERE gate switched from a
Posted_Date reference to a new never-null FilterDate column (same @filterDateFrom/@filterDateTo
params, same @useDateFrom/@useDateTo bits). safesql runs read-only.

### A04 · Insecure Design — Applicable, PASS
The redesign strengthens correctness: windowed runs no longer silently drop 100% of unpaid-installment
fund assignments (a Sev 1 - Business Stopping defect). FilterDate is internal-only (hidden from CSV
via explicit projection), so consumers of the export are unchanged. Auctifera__Posted_Date__c stays
NULL on unpaid — no downstream contract change.

### A05 · Security Misconfiguration — N/A

### A06 · Vulnerable & Outdated Components — N/A
### A07 · Identification & Auth Failures — N/A

### A08 · Software & Data Integrity Failures — Applicable, PASS
This PR CLOSES an A08 gap. Pre-fix: any windowed extraction dropped 141+ unpaid fund assignments
in Illinois (measured by dev), while opportunity_payments.sql + pledge_payments_unpaid_charges.sql
did emit their unpaid rows for the same window — the export was structurally divergent (an
Opportunity received an OppPayment and a Charge but no fund assignment). Post-fix: the 3 files
keep or drop the same pledge rows for the same window.

### A09 · Security Logging & Monitoring Failures — N/A
### A10 · Server-Side Request Forgery — N/A

## Summary
- 4 Applicable · 4 PASS · 0 FAIL · 6 N/A
- No security defect raised. No blocker.
