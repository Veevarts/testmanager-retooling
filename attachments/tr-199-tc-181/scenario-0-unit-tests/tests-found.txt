TC-181 — TESTS ANADIDOS POR EL PR #183

  src/.../query-regression/contact-solicit-code-query.spec.ts   (nuevo, unitario, 104 lineas)

  Lee el texto de la consulta y fija:
    - Implementation_External_ID__c = csc.ID, con cabecera SIN namespace
    - las dos ramas del lookup de constituyente (individuo -> Contact__c,
      organizacion o grupo -> Household_Organization__c)
    - la AUSENCIA de row gate sobre las banderas del constituyente
    - el grano: una fila por registro de CONSTITUENTSOLICITCODE
    - que NO reaparezca ninguna clave posicional: ni ROW_NUMBER ni el literal 'SC_'

  Detalle de diseno que merece mencion: el spec separa los comentarios del SQL ejecutable
  antes de buscar, porque el encabezado del archivo NOMBRA las construcciones que los tests
  prohiben. Sin ese filtro, el propio comentario haria fallar la asercion.

EJECUCION EN LA RAMA DEL PR
  npm run test:queries     655 passed / 830 total (175 skipped = contenedor)   EXIT 0
  Ver test-run-log.txt.

  Suite en contenedor: ver integration-docker.log y la nota de entorno en
  regression-results.txt. El archivo cambiado NO tiene spec de integracion (cero coincidencias
  de "solicit" en la salida de esa suite), asi que no cubre este cambio; se ejecuta solo como
  comprobacion de radio de impacto.
