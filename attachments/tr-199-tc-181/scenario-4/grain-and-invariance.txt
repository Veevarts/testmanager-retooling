TC-181 escenario 4 — EL GRANO NO CAMBIA Y LO YA EMITIDO QUEDA INTACTO
Fuente: Long Island · base 8b522c37 vs head ebaaef8 · 2026-09-23

PRUEBA ESTRUCTURAL (mas fuerte que un muestreo)
  El diff del archivo, ignorando comentarios, es EXACTAMENTE la insercion de tres columnas
  en la lista de proyeccion. Ni una linea de FROM, JOIN, WHERE o GROUP BY cambia:

    base 16 lineas -> PR 54 lineas, y el delta funcional son 14 lineas, todas dentro del
    SELECT. El resto son comentarios de encabezado.

  Las dos uniones resuelven un solo padre por clave primaria:
    CONSTITUENTSOLICITCODE csc JOIN CONSTITUENT c ON c.ID = csc.CONSTITUENTID
    CONSTITUENTSOLICITCODE csc JOIN SOLICITCODE  sc ON sc.ID = csc.SOLICITCODEID
  Un INNER JOIN por clave primaria no puede multiplicar filas. El grano — una fila por
  registro de CONSTITUENTSOLICITCODE — esta garantizado por construccion, no por muestreo.

MEDICION

  Version                     Filas   Veredicto
  --------------------------  -----   ---------
  base 8b522c37                 959   -
  PR   ebaaef8                  959   PASS, delta 0
  Declarado por el autor        959   PASS exacto

  Cero filas anadidas, cero eliminadas.

COLUMNAS PREEXISTENTES
  Como el cambio solo anade columnas a la proyeccion y no toca ninguna clausula que
  seleccione filas, las columnas que ya existian no pueden cambiar de valor: se leen de las
  mismas filas de las mismas tablas por las mismas uniones. Se verifico ademas con huella
  CHECKSUM_AGG sobre la terna (id externo, constituyente, codigo), identica entre corridas.

  Las tres columnas nuevas son lo unico que difiere entre las dos salidas.

RADIO DE IMPACTO
  Se busco quien referencia este archivo en el repositorio:
    - src/.../catalog/query-metadata.generated.json  (catalogo generado)
    - src/.../query-regression/contact-solicit-code-query.spec.ts  (el spec nuevo del PR)
  NINGUN pipeline de la aplicacion lo consume y ninguna otra consulta lee su salida. El
  radio de impacto es cero, tal como el autor declara.

OBJETIVO CUMPLIDO: SI
