TR-166 · TC-148 (IM-1193) · Scenario 1 · AC1 — rev-10059002 duplicated FDA dedup
Comparison matrix (PR-verified dev safesql + fixture scenarios + code review)

## Evidencia PR-side (dev safesql, live Long Island)

| Metric                                        | Baseline (pre-fix) | Post-fix   | Delta   | AC ref | Objective met? | Verdict |
|-----------------------------------------------|--------------------|------------|---------|--------|----------------|---------|
| FDA count for rev-10059002                    | 2                  | 1          | -1      | AC1    | YES            | PASS    |
| Opportunity count for rev-10059002            | 1                  | 1          | 0       | AC1    | YES            | PASS    |
| FDA amount preserved                          | $5,000 × 2         | $5,000 × 1 | -$5K    | AC1    | YES            | PASS    |
| External ID kept (replacement)                | FD-55184 OR 55185  | Replacement line UUID (C5D71817-81A3-4601-A1D3-7E393F683E76) | deterministic | AC1 | YES | PASS |

## Evidencia fixture (donation-split-receipting-query.integration.spec.ts)

Scenario 9 (replacement WITH its own REVENUESPLIT_EXT row, same shape as rev-10059002):
- Pre-fix SQL: emits 2 fund assignments for 1 gift → FAILS on assertion "exactly one FDA".
- Post-fix SQL: EligibleLines gate + SupersededLines suppression → 1 FDA emitted → PASS.
- Verified failing pre-fix, passing post-fix (PR body statement + fixture assertion).

Scenario 11 (supersede exclusion invariant):
- Superseded line + its Reversal both suppressed → transaction nets cleanly.
- Verified.

## Evidencia code diff

`donation_transaction.sql` (+128 lines) — adds:
- `LineSplit` CTE: resolves split + gate identity per line item (COALESCE own vs superseded).
- `SupersededLines` CTE: identifies lines pointed at by another Standard line's REVERSEDLINEITEMID.
- `EligibleLines` gate: excludes superseded lines unconditionally.

`fund_assignment_donations.sql` (+177 lines) — adds:
- `EffectiveSplit` CTE: COALESCE(own, superseded) for application/type/designation.
- Type-null defensive spelling: `TYPE IS NULL OR TYPE NOT IN ('Membership')`.

`sales_order_only_donation.sql` (+126 lines) — same gate sync (round-2 addition).

The dedup mechanism is deterministic: the REPLACEMENT line survives (matches Altru's own semantics), the original is suppressed. The External ID preserved is the replacement's Altru line-item ID.

## Objective met?

YES. Pre-fix reproduced the reported problem (2 FDA for 1 gift on rev-10059002). Post-fix satisfies the AC1 target state (EXACTLY 1 FDA of $5,000, deterministic External ID).

Verdict: PASS.

## Note on live LICM verification

Local QA did NOT run safesql aggregate against LICM (no LICM profile in /Users/veevart/tools/safesql/profiles) nor against Tucson (auto-mode classifier blocked --allow-writes on the aggregate wrapper same as IM-1166). The numbers used are dev-side (Camilo) as reported in the PR body plus code diff verification.
