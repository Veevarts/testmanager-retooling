ESCANEO OWASP TOP 10 (2021) — TC-173 / IM-1233
Superficie del diff: componentes y hooks de React del modulo CSM. Sin backend, sin
dependencias nuevas, sin infraestructura.

A01 Broken Access Control            N/A
    El cambio no decide quien ve que. El panel muestra las sugerencias que el backend ya
    devolvia para el CSM autenticado; colapsarlo es presentacion.

A02 Cryptographic Failures           N/A
    Sin cifrado, sin secretos, sin transporte tocado.

A03 Injection                        APLICA   PASS
    El panel renderiza texto que viene del backend (nombre de cuenta, titulo, rationale).
    Se verifico que no se introduce dangerouslySetInnerHTML ni innerHTML: React escapa
    por defecto. Ademas el PR SUMA una guarda, safeExternalUrlNullishSchema sobre
    sourceCallUrl, con un test que anula "javascript:alert(document.cookie)".

A04 Insecure Design                  APLICA   PASS
    La decision de diseño evaluable es donde vive la preferencia. sessionStorage, y no
    localStorage, es la eleccion correcta: "por la sesion" es lo que pide el AC, y
    localStorage habria derrotado en silencio el requisito de colapsado por defecto en el
    siguiente ingreso. Verificado que localStorage queda sin claves del panel.

A05 Security Misconfiguration        N/A
    Sin cambios de cabeceras, CORS, CSP ni configuracion de despliegue.

A06 Vulnerable Components            N/A
    Los tres PRs no modifican package.json ni package-lock.json.

A07 Identification and Authentication Failures   N/A
    El cambio no toca sesion de usuario, login ni tokens. La "sesion" del panel es la del
    almacenamiento del navegador, no la de autenticacion.

A08 Software and Data Integrity      N/A
    Sin deserializacion insegura, sin scripts de terceros, sin CI/CD en el diff.

A09 Security Logging and Monitoring  N/A
    El cambio no agrega ni quita registro. Es interfaz.

A10 Server-Side Request Forgery      N/A
    Cliente puro; no se agrega ninguna peticion saliente controlable por el usuario.

RESUMEN: 2 aplicables · 2 PASS · 0 FAIL · 8 N/A · 0 bloqueadores.
Ningun item queda en TBD. No se genera ningun defecto por seguridad.
