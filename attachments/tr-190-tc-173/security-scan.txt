=== 1. Secretos o credenciales en el diff d3a0127..403ab82
(vacio arriba = limpio)

=== 2. Almacenamiento del navegador introducido
+    "javascript:alert(document.cookie)",
+    // "For the session" is the requirement. localStorage would carry the open
+    expect(localStorage.getItem(MY_DAY)).toBeNull();
+    expect(sessionStorage.getItem(ACCOUNT)).toBe("true");
+    expect(sessionStorage.getItem(MY_DAY)).toBe("true");
+    expect(sessionStorage.getItem(MY_DAY)).toBeNull();
+    return sessionStorage.getItem(key) === "true";
+    sessionStorage.setItem("csm.my-day.suggested-panel-open", "true");

=== 3. HTML sin sanitizar
(vacio arriba = limpio)

=== 4. Cambios de dependencias
ninguno en estos PRs
