Static simulation of constituency-query.spec.ts assertions vs HEAD constituency.sql
====================================================================================

9/9 assertions PASS (all 6 `describe` blocks + sub-assertions collapsed into 9 checks)

  PASS  IT1 sources V_QUERY_CONSTITUENCY vq
  PASS  IT1 no BOARDMEMBERDATERANGE / VOLUNTEER / STAFF / FUNDRAISER
  PASS  IT1 no 'Board Member' AS Constituency__c hardcoded
  PASS  IT1 no CONSTITUENCYCODE cc
  PASS  IT2 WHERE vq.ISACTIVE = 1 (exclude retired defs)
  PASS  IT3 COALESCE + SYS_ + CONSTITUENTID + CONSTITUENCYDEFINITIONID
  PASS  IT4 ROW_NUMBER PARTITION BY Implementation_External_ID__c + WHERE RowRank = 1
  PASS  IT5 vq.CONSTITUENCY AS Constituency__c + DATEFROM + DATETO
  PASS  IT6 CONSTITUENTID = ct.ID + LEFT JOIN CONSTITUENTHOUSEHOLD + ELSE ch.HOUSEHOLDID + ct.ISCONSTITUENT = 1

Method: python re.search over comment-stripped SQL (mirrors stripComments util
from the repo). Whitespace-tolerant matches identical to the spec's regexes.
No jest / node runtime available here (MigrationToolETL not cloned), but the
assertions are pure text-contract locks on the SQL file; simulation is
equivalent to running the tests.

PR body claims `npm run test:queries` green (245 pass, incl. this spec).
Not blocking — upstream CI gate enforces it.
