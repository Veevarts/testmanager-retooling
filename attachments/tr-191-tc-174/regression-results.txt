Resultados de la matriz de regresion
====================================

TECNICA
  1-8, 9   PASS. Fuente de cada uno en la carpeta del escenario correspondiente. Los que
           corrieron contra base lo hicieron sobre un Postgres real local construido desde
           las migraciones de este arbol (347/347), con mutaciones que prueban que esos
           checks pueden ponerse en rojo.

  10       ABIERTO, no es un defecto nuevo de este PR: GET /csm/workspace/my-day devuelve
           227 acciones `suggested` sin tope ni clausula de lote, mientras el panel del dia
           muestra 5 como maximo. Es la entrada 18 del backlog del repo. La pantalla nueva
           ya no lo pinta, asi que hoy no es visible para el CSM; un cliente cacheado si lo
           ve (medido: 228 tarjetas). Detalle y numeros en scenario-9/.

  11       FAIL. La pestaña Deleted etiqueta "Task" a una sugerencia declinada porque el
           frontend decide con `kind` (que tras declinar vale 'task' para todo) en vez de
           con `stateBeforeDelete`, el campo que el backend agrego exactamente para eso y
           que el esquema del frontend ni siquiera parsea. Correccion de dos lineas.
           Detalle en scenario-3/defect-deleted-tab-labels-a-declined-suggestion-as-task.txt

SEGURIDAD
  S1       ABIERTO y PREEXISTENTE, no introducido aqui. `ownerId` se toma del que llama y
           solo se verifica el grupo `csm-team`, asi que un miembro del equipo puede leer y
           accionar tareas de otro CSM. Esta declarado en la entrada 1 del backlog del repo
           como modelo de producto vigente (la propia pantalla tiene un selector "View as"
           para ver la cola de otro CSM) y su cierre exige cambio de contrato y de frontend.
           Esta corrida se hizo precisamente asi: sesion admin, viendo como Emily Clever.
           No se abre defecto porque no es regresion de este cambio; queda registrado.

  S2       El motivo del decline se manda al backend y no se vuelve a pintar en ninguna de
           las superficies revisadas; el titulo y la descripcion se renderizan como texto
           (React escapa por defecto). Sin inyeccion observable.

  S3       Los identificadores de accion son UUID v4 y las rutas los validan con ParseUUIDPipe:
           un id invalido no llega al repositorio.

  S4       Ni el diff ni esta evidencia contienen credenciales, tokens ni datos personales.
           El token de sesion se uso desde el navegador y NO se copio a ningun archivo.
           Los nombres de cuenta que aparecen son datos sembrados del entorno de dev.

  S5       Soltar la sugerencia de otro CSM en una regeneracion forzada queda atribuido en
           `task_suggestion_batch_items.released_by`, con FK a csm_os.users; la corrida
           automatica queda en NULL. Verificado en el fixture.
