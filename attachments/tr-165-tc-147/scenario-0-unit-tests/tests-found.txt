Unit + query-regression tests added by PR #165 (mtoolet)
Executed 2026-08-18 via `npm run test:queries` on branch story/10-379-donations-sin-IM-1166

Files added or modified in the PR (test-related):
  server/src/data-migration/infrastructure/destination/sql-server/query-regression/
    · pos-purchase-donation-no-order-query.spec.ts                 (+411)
    · pos-purchase-donation-no-order-query.integration.spec.ts     (+411)
    · query-chain.live.spec.ts                                     (+62)
    · sql-server-query-fixture.ts                                  (+44)
  server/src/data-migration/infrastructure/destination/sql-server/queries/
    · fund-assignment/fund_assignment_donations.sql                (+18/-1)
    · opportunity-donation/donation_transaction.sql                (+17/-1)
    · pos-purchase/sales_order_only_donation2.sql                  (+391)  NEW

Local test-run result:
  Test Suites: 16 skipped, 43 passed, 43 of 59 total
  Tests:       128 skipped, 435 passed, 563 total
  Time:        ~8s

Skipped: the 16 `.integration.spec.ts` suites that require Docker + testcontainers
(activated with RUN_DOCKER_SQL_QUERY_TESTS=1). Docker was available locally but the
CI-only integration suites were skipped to keep the QA cycle fast; CI on the PR head
shows both `Lint, test, and build` runs SUCCESS.

AC coverage matrix:
  AC1 (populate External ID for 10,379 LICM donations)
    → covered by `pos-purchase-donation-no-order-query.spec.ts` static assertions +
      integration spec that runs the SQL against a Docker MSSQL fixture with orderless
      donations, asserts the synthetic key is emitted and byte-identical between the
      Opportunity side and the POS side.
  AC2 (unicidad + no colisiones)
    → covered by the CTE-identity spec + the bijection assertions
      (Opportunity ↔ POS synthetic key count match on both sides).
  AC3 (cascade Pledges/Payments)
    → covered by:
        pledges — sales_order_only_pledge.sql already mints its own key (pre-existing)
        payments — opportunity_payments.sql carries no POS lookup (documented as out
                   of scope in the PR body with the query name).
      No direct test needed; the fix does not touch pledge or payment queries.
  AC4 (regression / idempotency)
    → covered by:
        - CTE-identity spec: asserts the ten shared CTEs of the two files stay byte-
          identical (after whitespace normalize) so a one-sided edit fails immediately.
        - Cast-per-branch spec: asserts every COALESCE branch of vnfp__POS_Purchase__c
          is CAST/CONVERT to NVARCHAR to prevent the SQL Server type-precedence trap
          (IM-770).
        - Emission-gate spec: asserts the trailing WHERE + join block of both files
          stays in step (with exactly one allowed extra predicate on the no-order file).
        - query-chain.live.spec.ts: refund fund-assignment guard that asserts 0
          Opportunities with unresolved sales orders on the refund side.

Coverage gaps: none observed. Dev added extensive mutation-tested regression
assertions across two follow-up commits (a510028 + 707b288 + 770ebce). The unit-test
matrix is deeper than typical for this kind of query fix.
