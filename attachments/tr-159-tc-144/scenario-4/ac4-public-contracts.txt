AC4 — Public widget contracts (eligibility, render, submit, impression, dismissal) survive the migration byte-for-byte on the wire
Executed 2026-08-14 vs main HEAD (live post-migration).

------------------------------------------------------------------------------
Endpoints exercised (bundle in prod = index-9Qd0lrHV.js, DynamoDB-backed)
------------------------------------------------------------------------------

| Endpoint                                          | Method | Sample response                                                                | Verdict |
| ------------------------------------------------- | ------ | ------------------------------------------------------------------------------ | ------- |
| /api/surveys/general/eligible                     | GET    | 200 · `[]` for answered respondent · Survey#1 shape for fresh respondent (TR-158 evidence) | PASS |
| /api/features/:id/surveys?respondentId=&clientOrgId= | GET | 200 · `[]` (feature path)                                                       | PASS |
| /api/features/:id/respondent-state/:respondentId  | GET    | 200 · full JSON: `{feature:{id,slug}, respondentId, global:{...}, surveys:[{surveyId, active, limits:{...}, counters:{impressionCount,todayImpressionCount,dismissalCount,dismissalCountAllTime,responseCount,lastRespondedAt}, eligible, reason}]}` | PASS |
| /api/responses                                    | POST   | 201 with response id (TR-158 evidence — live submit id=2)                       | PASS |
| /api/features/:id/impressions                     | POST   | 400 in after-login mode (IM-1185 preexisting); 201 in feature-trigger mode      | PASS (contract), FAIL by IM-1185 (not this release) |
| /api/features/:id/dismissals                      | POST   | Same as impressions                                                             | PASS (contract), FAIL by IM-1185 |

------------------------------------------------------------------------------
Persistence roundtrip proof
------------------------------------------------------------------------------

Response submitted via VNFP live modal in TR-158 (id=2, score=9, respondent
005Rt00000XjqlyIAB, comment "QA TC-143 TR-158 · VNFP scratch...") is still
readable through:
  · Admin API (GET /api/admin/responses returns count=2)
  · Detail drawer (TR-158 screenshot 03-response-2-detail-vnfp.png)
  · CSV export (TR-158 05-responses-export.csv row 2)
  · Public /respondent-state (impressionCount=3, responseCount=1, lastRespondedAt matches)

DynamoDB is serving reads and writes end-to-end without any observable
regression in the public contract.

------------------------------------------------------------------------------
Contract fidelity vs pre-migration SQLite
------------------------------------------------------------------------------

The PR #45 body promises: "Existing API field names and route contracts are
preserved." Verified today by:
  · apiBoundary.test.ts (23KB, 6 integration tests) exercises the exact
    contract shape with an INJECTED IN-MEMORY store — tests are the frozen
    contract, dynamodbStore is a swap-in adapter behind the same interface.
  · Live probe today returns the same JSON shapes.

------------------------------------------------------------------------------
Verdict
------------------------------------------------------------------------------

PASS. Public contracts survived the migration. The IM-1185 400 on after-login
impressions/dismissals is preexisting (verified in TR-158) and does not
regress this release.
