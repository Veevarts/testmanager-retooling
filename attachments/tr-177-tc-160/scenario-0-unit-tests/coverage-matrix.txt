AC <-> test coverage matrix — IM-1251 / TC-160 / PR survey-tool 72
===================================================================

| AC de la historia | Tests del dev que lo cubren | Tests QA de esta corrida | Cobertura |
|---|---|---|---|
| AC1 una fila por respondent con >=1 actividad | respondentEngagement.test.ts + apiBoundary "summarizes respondent engagement" (end-to-end) | qaTc160 "emits exactly one row per respondent across responses, impressions and dismissals" | CUBIERTO |
| AC2 Responded Yes/No | respondentEngagement.test.ts | qaTc160 "marks Yes with at least one response and No with events only" | CUBIERTO |
| AC3 totales de impressions y dismissals exactos | respondentEngagement.test.ts + apiBoundary (asserta las filas completas) | qaTc160 "counts each event type independently" | CUBIERTO |
| AC4 combinar multi-fuente sin doble conteo | respondentEngagement.test.ts | qaTc160 "does not multiply rows when the same respondent has many events" + el test de cruce de caso entre ingests | CUBIERTO |
| AC5 excluir actividad de otra org o survey | apiBoundary: siembra 00d-other y asserta que no aparece en el alcance de 00d-demo; All organizations devuelve 3 filas | qaTc160 "keeps the same respondent ID in two organizations as two separate rows" | CUBIERTO |
| AC6 empty state sin error | dashboard.test.ts (cliente, helpers) | qaTc160 "returns zero rows and zero unattributed for an empty selection" (nivel agregador) | CUBIERTO a nivel dato; el empty state VISUAL no se verifico (sin entorno desplegado) |
| AC7 acceso no autenticado denegado; publicos sin cambio | NINGUNO automatizado: "requires a survey for respondent engagement" asserta 400, no 401, porque el harness inyecta el store y no ejerce auth real | Verificacion EN VIVO por QA con servidor local: 401 en todos los casos (scenario-5/auth-boundary-live.txt) | GAP en tests automatizados, CUBIERTO por evidencia live de QA |

GAPS DE COBERTURA DECLARADOS:
  1. **AC7 no tiene test automatizado.** El unico test que toca el endpoint sin credenciales
     asserta 400 (validacion de surveyId), no 401, porque corre sobre el harness con el store
     inyectado y sin el middleware real. El PR body lo reconoce como verificacion manual local.
     QA lo cubrio ejecutando el servidor real y observando 401 (ver scenario-5).
     Recomendacion para el dev: agregar un test que monte requireAdminAuth y asserte 401.
  2. **El empty state visual (AC6) no se pudo observar**: requiere el dashboard desplegado.
     Queda cubierto a nivel de dato (el agregador devuelve rows vacio y el endpoint responde 200).
