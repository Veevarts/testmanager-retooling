GATES SUMMARY — IM-1082 IC 3.12 v11 — PR #254 @ merge 1552e7a7 (base 545e2cb0 = IM-1081 #253)
CODE PR spec-only (reused T6 anon apex). Evidence PRIMARY = code.

GATE                                              RESULT        LOG
tsc --noEmit                                      exit 0        gates/tsc.log
jest demo-company-info-mock-contact.spec.ts       8/8 PASS      gates/spec-3-12.log
jest ic-seeds validity + publish CLI              101/101       gates/seeds-validity.log
eslint touched .spec.ts                           0 errors      gates/eslint.log
jest full suite                                   1908 pass /   gates/full-jest.log
                                                  1 skip /
                                                  0 FAIL
                                                  (179 suites)

3.12 spec.ts (8 assertions, all PASS):
  1 parses against publish-plan CLI schema; case.version==11, template.version==11, hybrid
  2 case.version == template.version (lockstep)
  3 decomposes into one action per step, strictly ordered 1..7
  4 Company Information T6: required company name static; optional fields via describe-guarded
    dynamic put — asserts getDescribe().fields.getMap(), isUpdateable(), co.put(tok, value); String.isBlank
  5 Company Information and Mock Contact kept as SEPARATE T6 seed steps
  6 NPSP rollups deploy references all 18 DeveloperNames; v11 tolerates absence — asserts deploy
    contains 'absent' and NOT 'precondition failed'
  7 every T6 step pure Apex — no HTTP callout / Data API / session theft
  (+ manual/other structural assertions)

NOTE — no live IE run in this QA:
  Dev verified live end-to-end on CamiloDevOrg (org 00Dfj00000QJMx7EAH) via sf-CLI anonymous Apex
  (AWS SSO expired -> run-plan-local CLI cannot boot Secrets Manager, so real Apex was driven directly):
  18/18 rollups present + already enabled; the dynamic company put wrote website/telephone; test
  mutations cleaned up. Cross-version re-run surfaced NO drift; version bumped 10->11 (lockstep) anyway.
  MigrationApp-dev seed v11 PENDING-CREDS (append-only). Live = dev-proven / PENDING-CREDS, no premature PASS/FAIL.
