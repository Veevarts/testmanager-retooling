AC5-a Obsolete Sync page is unavailable in navigation and via direct URL
Executed 2026-08-14 (dev + stg live).

## Navigation link removed

AdminLayout.tsx nav links (lines 75-104):
  Subjects | Surveys | Dashboard | Organizations | Settings

NO "Sync" link. Confirmed live in dev AND stg via DOM `a[href^="/admin"]`
inspection — hrefs list:
  /admin/features · /admin/surveys · /admin/dashboard · /admin/organizations · /admin/settings

## SyncPage.tsx file DELETED in the diff

`gh pr diff 56 --repo Veevarts/survey-tool` shows:
  diff --git a/client/src/pages/SyncPage.tsx b/client/src/pages/SyncPage.tsx
  deleted file mode 100644
  index f71db52e..00000000
  --- a/client/src/pages/SyncPage.tsx
  +++ /dev/null
  @@ -1,222 +0,0 @@

222 lines removed. The file is gone from the codebase.

## Direct URL /admin/sync → redirects to /admin/dashboard

App.tsx:55:
  <Route path="sync" element={<Navigate to="/admin/dashboard" replace />} />

Live evidence:
  · Dev: navigate to https://surveytool.dev.veevart.ai/admin/sync
         → pathname becomes /admin/dashboard, Dashboard heading renders (no 404, no error).
  · Stg: navigate to https://surveytool.stg.veevart.ai/admin/sync
         → pathname becomes /admin/dashboard, same behavior.

The AC said "unavailable"; both interpretations (404 or safe redirect) satisfy it.
The dev team chose safe redirect so that any bookmarks / external links pointing
to /admin/sync don't leave the admin in an error state.

Verdict: AC5-a PASS.
