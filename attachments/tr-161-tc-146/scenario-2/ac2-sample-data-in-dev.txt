AC2 Development sample organizations are loaded on demand and available as audience
Executed 2026-08-14 (dev live).

## Load sample data behavior (live)

Before click:  2 organizations (Auctifera + VNFP, both from real backend)
After click:   6 organizations (2 backend + 4 local samples)

Local samples added:
  · Art Museum         (00D000000000001AAA · sample from localSampleOrganizations.ts)
  · Botanical Garden   (subdomain botanical-scratch)
  · City Gallery 10    (00D000000000010AAA)
  · City Gallery 2     (subdomain gallery-two-scratch)

Row action column for sample rows shows "Local sample" (no Edit/Delete/Deactivate)
— confirms samples are CLIENT-ONLY and never persisted to the backend.

Success toast: "Local sample organizations loaded and available in survey audiences."

## Persistence check

- Backend GET /api/admin/organizations still returns the 2 real orgs only
  (sample orgs have negative ids -1..-4 and live in sessionStorage
  `veevart:surveyTool:localSampleOrganizations=enabled`).
- Sample orgs will NOT appear in stg or prd because the "Load sample data"
  button is gated to `environment === "development"`.

Screenshots:
  · scenario-2/dev-orgs-with-sample-loaded.png

Code:
  · lib/localSampleOrganizations.ts (37 lines) — `mergeLocalSampleOrganizations`,
    `enableLocalSampleOrganizations`, `localSampleOrganizationsEnabled`.
  · Unit tests: lib/localSampleOrganizations.test.ts (2 tests: adds without
    replacing API orgs; dedups by identifier).

Verdict: AC2 PASS on dev. Persistence contained to sessionStorage in the
browser; no server writes.
