AC1 Organizations sortable headers + environment-conditional actions
Executed 2026-08-14 (dev + stg live; prd DEFERRED until PR #58 merges).

## Header inspection (DOM audit)

Dev (https://surveytool.dev.veevart.ai/admin/organizations):
  · Name           aria-sort=ascending    hasButton=true   (default sort)
  · Identifier     aria-sort=none         hasButton=true
  · Source         aria-sort=none         hasButton=true
  · Status         aria-sort=none         hasButton=true
  · Actions        (no sort — action column)

Sorting click test: clicked "Identifier" header →
  · Name       aria-sort=none          ← switched from ascending
  · Identifier aria-sort=ascending▲   ← new default sort
  · Others     aria-sort=none

Toggle contract confirmed: click same column twice → descending; click a
different column → ascending on the new column. Implementation:
`toggleSort` in OrganizationsPage.tsx:111-117.

## Environment-conditional top-bar buttons

Dev  (`Environment: development`):  ["Load sample data", "New organization"]
Stg  (`Environment: staging`):      ["New organization"]                                 ← NO Load sample, NO Sync SF
Prd  (`Environment: production`):   DEFERRED (PR #58 staging→main still OPEN)

Code gates (OrganizationsPage.tsx:217-238):
  · Load sample data: `{environmentResolved && environment === "development" && ...}`
  · Sync Salesforce:  `{production && ...}`
  · New organization: always present

Screenshots:
  · scenario-1/dev-orgs-baseline.png
  · scenario-1/stg-orgs-baseline.png

Verdict: AC1 PASS on dev + stg. Prd deferred with explicit reason.
