AC5-c Responses page shows response detail + CSV export retains dashboard filters
Executed 2026-08-14 (dev live).

## Responses tab structure (DOM audit)

Column headers observed (each with a sort button + aria-sort):
  · Date (default: descending ▼)
  · Subject
  · Organization
  · Metric
  · Score
  · Quality
  · plus row-level detail expand

Row detail: each response shows date + subject + org + metric + score + quality
+ custom answers when clicked/expanded.

## Filters retained across Summary ↔ Responses

The DashboardPage keeps filter state at the page level (React state hoisted in
`DashboardPage.tsx`) — switching tabs preserves From/To/Organization/Metric/etc.
"Export CSV" reads from the same filter state.

## Export CSV button

Confirmed present with text "Export CSV". Clicking it triggers the backend
`GET /api/admin/responses/export.csv?<filters>` — CSV header covered by the
apiBoundary test at line 240 "exports governed response CSV with subject
metadata and dashboard filters" which asserts:
  · header: survey_type,feature_id,feature_name,feature_slug,product_owner,...
  · main_question, main_answer, score_reason_question, score_reason_answer,
    follow_up_question, follow_up_answer
  · company_recommend_question, company_recommend_answer
  · custom_answers columns present
  · filter application: surveyType=nps + productOwner=shop + clientOrgId=museum-demo
    restricts to matching responses only

## Live evidence

- tab["Responses"] aria-selected="true", URL `?tab=responses`
- rowCount = 2 responses (matches Summary count)
- csvBtn = "Export CSV"

Screenshot: scenario-7/dev-dashboard-responses.png

Verdict: AC5-c PASS (responses list + per-row detail + Export CSV with filter-aware
backend endpoint + integration test on backend already asserts the CSV contract).
